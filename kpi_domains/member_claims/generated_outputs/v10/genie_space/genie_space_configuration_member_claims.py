# Databricks notebook source
# DBTITLE 1,Genie Space Configuration Tool
# MAGIC %md
# MAGIC # Genie Space Configuration — member_claims
# MAGIC
# MAGIC This notebook is a **modular configuration tool** for managing the Genie space. Edit the configuration cells below, then run the execution cells to create or update the space.
# MAGIC
# MAGIC ## Notebook Structure
# MAGIC | Cell | Purpose | Action |
# MAGIC |---|---|---|
# MAGIC | **Cells 2-7** | Configuration (edit these) | Define space settings, instructions, metric views, questions, SQLs, benchmarks |
# MAGIC | **Cell 8** | Helper functions | Builds the `serialized_space` JSON from your config |
# MAGIC | **Cell 9** | Create / Update space | Calls the Genie API to apply changes |
# MAGIC | **Cell 10** | Validate space | Reads back the space and displays a summary |
# MAGIC
# MAGIC ## How to Use
# MAGIC 1. Edit any configuration cell (Cells 2–7)
# MAGIC 2. Run **Cell 8** (helpers), then **Cell 9** (apply), then **Cell 10** (validate)
# MAGIC 3. To create a **new** space: set `SPACE_ID = ""` in Cell 2
# MAGIC 4. To **update** an existing space: set `SPACE_ID = "<existing_id>"` in Cell 2

# COMMAND ----------

# DBTITLE 1,Space Configuration
# =============================================================================
# SPACE CONFIGURATION
# Values populated from accelerator.yaml config
# =============================================================================

SPACE_TITLE = "member_claims_analytics_genie_v10"

SPACE_DESCRIPTION = "Validated Member Claims Genie Space for claim cost, claim volume, adjudication quality, provider participation, enrollment, line-of-business, and geography analytics using authoritative Databricks Metric Views."

# Set to "" to CREATE a new space, or an existing ID to UPDATE
SPACE_ID = ""
WAREHOUSE_ID = "2d8e531640ffa469"
PARENT_PATH = "/Workspace/Users/arun.wagle@databricks.com/databricks-aibi-design-first-accelerator/kpi_domains/member_claims/generated_outputs/v10/genie_space"
TABLE_IDENTIFIERS = ["aw_serverless_stable_catalog.aibi_member_claims.member_claims_metric_view_v10", "aw_serverless_stable_catalog.aibi_member_claims.member_claims_enrollment_metric_view_v10"]

print(f"Space: {SPACE_TITLE}")
print(f"Mode:  {'UPDATE existing' if SPACE_ID else 'CREATE new'}")

# COMMAND ----------

# DBTITLE 1,General Instructions
# =============================================================================
# GENERAL INSTRUCTIONS — Generated by LLM from KPI spec and schema profile
# =============================================================================

GENERAL_INSTRUCTIONS = """## Domain
This Genie Space provides validated healthcare member-claims analytics over two authoritative Databricks Metric Views: `member_claims_metric_view_v10` for claim-detail measures and `member_claims_enrollment_metric_view_v10` for enrollment/member distribution measures. Use it for claim volume, paid cost, adjudication quality, provider participation, enrollment, line-of-business, and geography questions.

## Metric View Catalog
- `aw_serverless_stable_catalog.aibi_member_claims.member_claims_metric_view_v10`: claim service-line grain; use for Total Claims, Total Claim Lines, Total Paid Amount, Average Paid per Claim, Denial Rate, Clean Claim Rate, Payment-to-Billed Ratio, Payment-to-Allowed Ratio, Average Paid per Member, Claims per Member, Lines per Claim, Inpatient Paid Amount, Outpatient Paid Amount, and Participating Provider Rate.
- `aw_serverless_stable_catalog.aibi_member_claims.member_claims_enrollment_metric_view_v10`: member enrollment span/event grain; use for New Member Enrollment, Active Members, and Members by Geography.

## Measures (always query with MEASURE(`name`))
- `Total Claims`: distinct claim count.
- `Total Claim Lines`: count of service lines.
- `Total Paid Amount`: paid dollars across claim lines.
- `Average Paid per Claim`, `Average Paid per Member`, `Claims per Member`, `Lines per Claim`: non-additive ratios/averages.
- `Denial Rate`, `Clean Claim Rate`, `Payment-to-Billed Ratio`, `Payment-to-Allowed Ratio`, `Participating Provider Rate`: non-additive percentage ratios; never sum these.
- `Inpatient Paid Amount` and `Outpatient Paid Amount`: filtered paid-dollar measures for Institutional and Professional claim types.
- `New Member Enrollment`, `Active Members`, `Members by Geography`: distinct member enrollment measures; do not sum across months as if members were additive.

## Dimensions and Filters
Claim dimensions include `Claim Type` (Dental, Institutional, Pharmacy, Professional, Vision), `Line Status` (Adjusted, Denied, Paid, Pending, Reversed), `Clean Claim Indicator`, `Benefit Category`, `Participating Provider`, `Service Date`, and `service_month`. Enrollment dimensions include `Enrollment Status`, `Line of Business` (Commercial, Exchange, Medicaid, Medicare, TRICARE), `Member State` (CA, FL, GA, NC, NY, OH, PA, TX), `Plan ID`, `Product ID`, `Effective Date`, and `service_month`.

## Query Rules
Always use Metric View syntax such as `SELECT MEASURE(`Total Paid Amount`) FROM ...`; do not reconstruct KPI formulas from raw tables. Use `GROUP BY ALL` when grouping by dimensions. Use `service_month` for monthly trends. Do not mix measures from different metric views in a single query. Exclude PMPM, Claims per 1,000 Members, Utilization Rate, High-Cost Member Count, Rolling 3-Month PMPM, and MoM Active Member Growth because they are documented as NOT_IMPLEMENTED for Genie Metric View querying in this version."""

print(f"General instructions: {len(GENERAL_INSTRUCTIONS):,} chars")

# COMMAND ----------

# DBTITLE 1,Metric View Descriptions
# =============================================================================
# METRIC VIEW DESCRIPTIONS
# =============================================================================

METRIC_VIEW_DESCRIPTIONS = {"aw_serverless_stable_catalog.aibi_member_claims.member_claims_enrollment_metric_view_v10": "Validated enrollment metric view at member enrollment span/event grain. It supports new enrollment, active member counts, and member geography distribution by enrollment status, line of business, member state, plan/product, and service month.", "aw_serverless_stable_catalog.aibi_member_claims.member_claims_metric_view_v10": "Validated claim-detail metric view at one row per claim service line/detail. It supports claim volume, paid amount, average paid, denial and clean-claim rates, payment ratio, inpatient/outpatient paid, and participating-provider analytics sliced by claim type, line status, benefit category, provider participation, and service month."}

print(f"Metric views: {len(METRIC_VIEW_DESCRIPTIONS)}")
for k in sorted(METRIC_VIEW_DESCRIPTIONS):
    print(f"  • {k}")

# COMMAND ----------

# DBTITLE 1,Sample Questions
# =============================================================================
# SAMPLE QUESTIONS — Appear in Genie chat UI as suggestions
# =============================================================================

SAMPLE_QUESTIONS = ["What is the total paid amount across all claims?", "How many distinct claims and claim lines are represented?", "What is the monthly trend in total paid amount?", "Show total paid amount by claim type.", "Which benefit categories have the highest total paid amount?", "What is the denial rate for Professional claims?", "Compare denial rate across claim types.", "What is the clean claim rate by clean claim indicator?", "Show payment-to-billed ratio and payment-to-allowed ratio by claim type.", "What are inpatient and outpatient paid amounts by service month?", "Which participating provider category has the highest participating provider rate?", "What is average paid per claim by benefit category?", "How many active members are there by line of business?", "What is the monthly trend in new member enrollment?", "Show members by geography for Texas and California.", "Compare active members across Commercial, Medicare, and Medicaid lines of business."]

print(f"Sample questions: {len(SAMPLE_QUESTIONS)}")
for i, q in enumerate(SAMPLE_QUESTIONS, 1):
    print(f"  {i:2d}. {q}")

# COMMAND ----------

# DBTITLE 1,Example Question SQLs (Instructions)
# =============================================================================
# EXAMPLE QUESTION SQLS — Teach Genie how to answer (part of instructions)
# =============================================================================

EXAMPLE_QUESTION_SQLS = [("What is the total paid amount across all claims?", "SELECT MEASURE(`Total Paid Amount`) AS total_paid_amount FROM `aw_serverless_stable_catalog`.`aibi_member_claims`.`member_claims_metric_view_v10`"), ("How many distinct claims and claim lines are represented?", "SELECT MEASURE(`Total Claims`) AS total_claims, MEASURE(`Total Claim Lines`) AS total_claim_lines FROM `aw_serverless_stable_catalog`.`aibi_member_claims`.`member_claims_metric_view_v10`"), ("What is the monthly trend in total paid amount?", "SELECT service_month, MEASURE(`Total Paid Amount`) AS total_paid_amount FROM `aw_serverless_stable_catalog`.`aibi_member_claims`.`member_claims_metric_view_v10` GROUP BY ALL ORDER BY service_month"), ("Show total paid amount by claim type.", "SELECT `Claim Type` AS claim_type, MEASURE(`Total Paid Amount`) AS total_paid_amount FROM `aw_serverless_stable_catalog`.`aibi_member_claims`.`member_claims_metric_view_v10` GROUP BY ALL ORDER BY total_paid_amount DESC"), ("Which benefit categories have the highest total paid amount?", "SELECT `Benefit Category` AS benefit_category, MEASURE(`Total Paid Amount`) AS total_paid_amount FROM `aw_serverless_stable_catalog`.`aibi_member_claims`.`member_claims_metric_view_v10` GROUP BY ALL ORDER BY total_paid_amount DESC LIMIT 10"), ("What is the denial rate for Professional claims?", "SELECT MEASURE(`Denial Rate`) AS denial_rate FROM `aw_serverless_stable_catalog`.`aibi_member_claims`.`member_claims_metric_view_v10` WHERE `Claim Type` = 'Professional'"), ("Compare denial rate across claim types.", "SELECT `Claim Type` AS claim_type, MEASURE(`Denial Rate`) AS denial_rate FROM `aw_serverless_stable_catalog`.`aibi_member_claims`.`member_claims_metric_view_v10` GROUP BY ALL ORDER BY denial_rate DESC"), ("What is the clean claim rate by clean claim indicator?", "SELECT `Clean Claim Indicator` AS clean_claim_indicator, MEASURE(`Clean Claim Rate`) AS clean_claim_rate FROM `aw_serverless_stable_catalog`.`aibi_member_claims`.`member_claims_metric_view_v10` GROUP BY ALL ORDER BY clean_claim_rate DESC"), ("Show payment-to-billed ratio and payment-to-allowed ratio by claim type.", "SELECT `Claim Type` AS claim_type, MEASURE(`Payment-to-Billed Ratio`) AS payment_to_billed_ratio, MEASURE(`Payment-to-Allowed Ratio`) AS payment_to_allowed_ratio FROM `aw_serverless_stable_catalog`.`aibi_member_claims`.`member_claims_metric_view_v10` GROUP BY ALL ORDER BY claim_type"), ("What are inpatient and outpatient paid amounts by service month?", "SELECT service_month, MEASURE(`Inpatient Paid Amount`) AS inpatient_paid_amount, MEASURE(`Outpatient Paid Amount`) AS outpatient_paid_amount FROM `aw_serverless_stable_catalog`.`aibi_member_claims`.`member_claims_metric_view_v10` GROUP BY ALL ORDER BY service_month"), ("Which participating provider category has the highest participating provider rate?", "SELECT `Participating Provider` AS participating_provider, MEASURE(`Participating Provider Rate`) AS participating_provider_rate FROM `aw_serverless_stable_catalog`.`aibi_member_claims`.`member_claims_metric_view_v10` GROUP BY ALL ORDER BY participating_provider_rate DESC LIMIT 10"), ("What is average paid per claim by benefit category?", "SELECT `Benefit Category` AS benefit_category, MEASURE(`Average Paid per Claim`) AS average_paid_per_claim FROM `aw_serverless_stable_catalog`.`aibi_member_claims`.`member_claims_metric_view_v10` GROUP BY ALL ORDER BY average_paid_per_claim DESC"), ("How many active members are there by line of business?", "SELECT `Line of Business` AS line_of_business, MEASURE(`Active Members`) AS active_members FROM `aw_serverless_stable_catalog`.`aibi_member_claims`.`member_claims_enrollment_metric_view_v10` GROUP BY ALL ORDER BY active_members DESC"), ("What is the monthly trend in new member enrollment?", "SELECT service_month, MEASURE(`New Member Enrollment`) AS new_member_enrollment FROM `aw_serverless_stable_catalog`.`aibi_member_claims`.`member_claims_enrollment_metric_view_v10` GROUP BY ALL ORDER BY service_month"), ("Show members by geography for Texas and California.", "SELECT `Member State` AS member_state, MEASURE(`Members by Geography`) AS members_by_geography FROM `aw_serverless_stable_catalog`.`aibi_member_claims`.`member_claims_enrollment_metric_view_v10` WHERE `Member State` IN ('TX', 'CA') GROUP BY ALL ORDER BY members_by_geography DESC"), ("Compare active members across Commercial, Medicare, and Medicaid lines of business.", "SELECT `Line of Business` AS line_of_business, MEASURE(`Active Members`) AS active_members FROM `aw_serverless_stable_catalog`.`aibi_member_claims`.`member_claims_enrollment_metric_view_v10` WHERE `Line of Business` IN ('Commercial', 'Medicare', 'Medicaid') GROUP BY ALL ORDER BY active_members DESC")]

print(f"Example question SQLs: {len(EXAMPLE_QUESTION_SQLS)}")
for i, (q, _) in enumerate(EXAMPLE_QUESTION_SQLS, 1):
    print(f"  {i:2d}. {q}")

# COMMAND ----------

# DBTITLE 1,Benchmark Questions
# =============================================================================
# BENCHMARK QUESTIONS — Evaluate Genie accuracy (not used for training)
# =============================================================================

BENCHMARK_QUESTIONS = [("How much has been paid on claims in total?", "SELECT MEASURE(`Total Paid Amount`) AS total_paid_amount FROM `aw_serverless_stable_catalog`.`aibi_member_claims`.`member_claims_metric_view_v10`"), ("Give me the overall count of claims and service lines.", "SELECT MEASURE(`Total Claims`) AS total_claims, MEASURE(`Total Claim Lines`) AS total_claim_lines FROM `aw_serverless_stable_catalog`.`aibi_member_claims`.`member_claims_metric_view_v10`"), ("Trend paid dollars by month of service.", "SELECT service_month, MEASURE(`Total Paid Amount`) AS total_paid_amount FROM `aw_serverless_stable_catalog`.`aibi_member_claims`.`member_claims_metric_view_v10` GROUP BY ALL ORDER BY service_month"), ("Break down paid dollars by the type of claim.", "SELECT `Claim Type` AS claim_type, MEASURE(`Total Paid Amount`) AS total_paid_amount FROM `aw_serverless_stable_catalog`.`aibi_member_claims`.`member_claims_metric_view_v10` GROUP BY ALL ORDER BY total_paid_amount DESC"), ("Rank benefit categories by claim paid amount.", "SELECT `Benefit Category` AS benefit_category, MEASURE(`Total Paid Amount`) AS total_paid_amount FROM `aw_serverless_stable_catalog`.`aibi_member_claims`.`member_claims_metric_view_v10` GROUP BY ALL ORDER BY total_paid_amount DESC LIMIT 10"), ("What percent of Professional claim lines are denied?", "SELECT MEASURE(`Denial Rate`) AS denial_rate FROM `aw_serverless_stable_catalog`.`aibi_member_claims`.`member_claims_metric_view_v10` WHERE `Claim Type` = 'Professional'"), ("Show denial percentage for each claim type.", "SELECT `Claim Type` AS claim_type, MEASURE(`Denial Rate`) AS denial_rate FROM `aw_serverless_stable_catalog`.`aibi_member_claims`.`member_claims_metric_view_v10` GROUP BY ALL ORDER BY denial_rate DESC"), ("Compare clean-claim performance by clean status.", "SELECT `Clean Claim Indicator` AS clean_claim_indicator, MEASURE(`Clean Claim Rate`) AS clean_claim_rate FROM `aw_serverless_stable_catalog`.`aibi_member_claims`.`member_claims_metric_view_v10` GROUP BY ALL ORDER BY clean_claim_rate DESC"), ("Show paid-to-billed and paid-to-allowed rates by claim category.", "SELECT `Claim Type` AS claim_type, MEASURE(`Payment-to-Billed Ratio`) AS payment_to_billed_ratio, MEASURE(`Payment-to-Allowed Ratio`) AS payment_to_allowed_ratio FROM `aw_serverless_stable_catalog`.`aibi_member_claims`.`member_claims_metric_view_v10` GROUP BY ALL ORDER BY claim_type"), ("How do institutional and professional paid amounts move over time?", "SELECT service_month, MEASURE(`Inpatient Paid Amount`) AS inpatient_paid_amount, MEASURE(`Outpatient Paid Amount`) AS outpatient_paid_amount FROM `aw_serverless_stable_catalog`.`aibi_member_claims`.`member_claims_metric_view_v10` GROUP BY ALL ORDER BY service_month"), ("Rank network participation categories by participating provider rate.", "SELECT `Participating Provider` AS participating_provider, MEASURE(`Participating Provider Rate`) AS participating_provider_rate FROM `aw_serverless_stable_catalog`.`aibi_member_claims`.`member_claims_metric_view_v10` GROUP BY ALL ORDER BY participating_provider_rate DESC LIMIT 10"), ("Average claim cost by benefit category please.", "SELECT `Benefit Category` AS benefit_category, MEASURE(`Average Paid per Claim`) AS average_paid_per_claim FROM `aw_serverless_stable_catalog`.`aibi_member_claims`.`member_claims_metric_view_v10` GROUP BY ALL ORDER BY average_paid_per_claim DESC"), ("Show membership counts split by LOB.", "SELECT `Line of Business` AS line_of_business, MEASURE(`Active Members`) AS active_members FROM `aw_serverless_stable_catalog`.`aibi_member_claims`.`member_claims_enrollment_metric_view_v10` GROUP BY ALL ORDER BY active_members DESC"), ("Trend new enrollments over service month.", "SELECT service_month, MEASURE(`New Member Enrollment`) AS new_member_enrollment FROM `aw_serverless_stable_catalog`.`aibi_member_claims`.`member_claims_enrollment_metric_view_v10` GROUP BY ALL ORDER BY service_month"), ("How many members are in TX and CA?", "SELECT `Member State` AS member_state, MEASURE(`Members by Geography`) AS members_by_geography FROM `aw_serverless_stable_catalog`.`aibi_member_claims`.`member_claims_enrollment_metric_view_v10` WHERE `Member State` IN ('TX', 'CA') GROUP BY ALL ORDER BY members_by_geography DESC"), ("Compare enrollment counts for Commercial, Medicare, and Medicaid.", "SELECT `Line of Business` AS line_of_business, MEASURE(`Active Members`) AS active_members FROM `aw_serverless_stable_catalog`.`aibi_member_claims`.`member_claims_enrollment_metric_view_v10` WHERE `Line of Business` IN ('Commercial', 'Medicare', 'Medicaid') GROUP BY ALL ORDER BY active_members DESC")]

print(f"Benchmark questions: {len(BENCHMARK_QUESTIONS)}")
for i, (q, _) in enumerate(BENCHMARK_QUESTIONS, 1):
    print(f"  {i:2d}. {q}")

# COMMAND ----------

# DBTITLE 1,Validate Configuration (DETERMINISM GATE)
# =============================================================================
# VALIDATE ALL LLM-GENERATED CONTENT AGAINST ACTUAL SCHEMA
# This cell MUST run before Create/Update to catch incorrect column references,
# broken SQL, and instructions that reference non-existent tables/columns.
# =============================================================================

import json
import os
import sys
import uuid
from databricks.sdk import WorkspaceClient

# Use SDK for API calls — NEVER raw requests + extracted token
# (raw requests violates safety rules and doesn't work in all contexts)
w = WorkspaceClient()

# ── Gate Checks integration — programmatic enforcement layer ──
# gate_checks.py provides runtime assertions that PREVENT deployment of
# empty/incomplete Genie spaces (no instructions, no tables, no questions).
_gate_checks_loaded = False
try:
    _templates_dir = os.path.dirname(os.path.abspath(__file__)) if '__file__' in dir() else None
    if _templates_dir and _templates_dir not in sys.path:
        sys.path.insert(0, _templates_dir)
    from gate_checks import (
        run_genie_predeploy_gates,
        validate_genie_from_api,
        write_ground_truth_validation,
        GateCheckError,
    )
    _gate_checks_loaded = True
    print("✅ gate_checks loaded — programmatic Genie enforcement active")
except ImportError:
    print("⚠️  gate_checks.py not found — using template assertions only (reduced safety)")

def validate_genie_config(
    table_identifiers: list,
    example_sqls: list,
    general_instructions: str,
    sample_questions: list,
) -> dict:
    """Deterministic validation of all LLM-generated Genie space content.

    Checks:
    1. All table_identifiers exist and are accessible
    2. All example SQL queries execute successfully
    3. Instructions reference actual column names (warning if suspicious)
    4. Sample questions are non-empty and distinct

    Returns: {status: PASS/FAIL, issues: [...], sql_results: [...]}
    """
    issues = []
    sql_results = []

    # 1. Validate table access
    print("Validating table identifiers...")
    for tbl in table_identifiers:
        try:
            spark.sql(f"DESCRIBE TABLE {tbl}").limit(1).collect()
            print(f"  \u2713 {tbl}")
        except Exception as e:
            issues.append(f"Table '{tbl}' not accessible: {e}")
            print(f"  \u2717 {tbl}: {e}")

    # 2. Validate example SQLs execute
    print("\nValidating example SQL queries...")
    for i, (question, sql) in enumerate(example_sqls, 1):
        try:
            result = spark.sql(f"SELECT * FROM ({sql}) _t LIMIT 1")
            cols = [f.name for f in result.schema.fields]
            sql_results.append({"idx": i, "question": question, "status": "PASS", "columns": cols})
            print(f"  \u2713 Q{i}: {question[:60]}... ({len(cols)} cols)")
        except Exception as e:
            issues.append(f"Example SQL #{i} failed: {question[:50]}... Error: {e}")
            sql_results.append({"idx": i, "question": question, "status": "FAIL", "error": str(e)})
            print(f"  \u2717 Q{i}: {question[:60]}... ERROR: {e}")

    # 3. Check sample questions
    if len(sample_questions) < 3:
        issues.append(f"Only {len(sample_questions)} sample questions (need >= 3)")
    if len(set(sample_questions)) != len(sample_questions):
        issues.append("Duplicate sample questions detected")

    # 4. Check instructions aren't empty/too short
    if len(general_instructions) < 100:
        issues.append(f"Instructions too short ({len(general_instructions)} chars) — need >= 100")

    status = "PASS" if not issues else "FAIL"
    print(f"\n{'\u2705' if status == 'PASS' else '\u274c'} Genie config validation: {status}")
    if issues:
        for issue in issues:
            print(f"   \u2022 {issue}")

    return {"status": status, "issues": issues, "sql_results": sql_results}


# Run validation
validation_result = validate_genie_config(
    table_identifiers=TABLE_IDENTIFIERS,
    example_sqls=EXAMPLE_QUESTION_SQLS,
    general_instructions=GENERAL_INSTRUCTIONS,
    sample_questions=SAMPLE_QUESTIONS,
)
assert validation_result["status"] == "PASS", (
    f"Genie config validation FAILED. Fix these issues before deploying:\n"
    + "\n".join(f"  - {i}" for i in validation_result["issues"])
)

# COMMAND ----------

# DBTITLE 1,Helper Functions
# =============================================================================
# HELPER FUNCTIONS
# Run this cell before Cell 9 (Create/Update) or Cell 10 (Validate).
# =============================================================================



def _sorted_hex_ids(n: int) -> list[str]:
    """Generate *n* sorted 32-char lowercase hex UUIDs."""
    return sorted(uuid.uuid4().hex for _ in range(n))


def build_serialized_space(
    general_instructions: str,
    metric_view_descriptions: dict[str, str],
    sample_questions: list[str],
    example_question_sqls: list[tuple[str, str]],
    benchmark_questions: list[tuple[str, str]],
) -> str:
    """
    Build the serialized_space JSON string from user-editable config.

    Returns a JSON string ready for the Genie Space API.
    All IDs are auto-generated, sorted, and guaranteed unique.

    GENIE API FORMAT REQUIREMENTS (discovered empirically):
    - version: MUST be 2 (version 0 or 1 rejected)
    - IDs: MUST be 32-char lowercase hex UUIDs WITHOUT hyphens
      (e.g., 'a1b2c3d4e5f6789012345678abcdef01', NOT 'a1b2c3d4-e5f6-...')
    - question: MUST be an array of strings (NOT a plain string)
    - sql: MUST be an array of strings (NOT a plain string)
    - content: MUST be an array of strings (NOT a plain string)
    - IDs within each section MUST be sorted lexicographically
    """
    # ═══ MINIMUM CONTENT ASSERTIONS (DETERMINISM GATE) ═══
    # These prevent deploying an incomplete Genie space.
    assert len(general_instructions) >= 100, (
        f"Instructions too short ({len(general_instructions)} chars). "
        f"Need >= 100 chars covering domain, measures, dimensions, and query rules."
    )
    assert len(metric_view_descriptions) >= 1, (
        f"No metric_view_descriptions provided. "
        f"Must include at least the primary metric view with its description."
    )
    assert len(sample_questions) >= 5, (
        f"Only {len(sample_questions)} sample questions. "
        f"Need >= 5 diverse questions covering different KPIs and dimensions."
    )
    assert len(example_question_sqls) >= 5, (
        f"Only {len(example_question_sqls)} example SQLs. "
        f"Need >= 5 validated SQL examples teaching Genie query patterns."
    )
    assert len(benchmark_questions) >= 3, (
        f"Only {len(benchmark_questions)} benchmark questions. "
        f"Need >= 3 benchmark Q+SQL pairs for accuracy evaluation."
    )

    # ---- Generate sorted IDs for every section ----
    sq_ids = _sorted_hex_ids(len(sample_questions))
    eq_ids = _sorted_hex_ids(len(example_question_sqls))
    bm_ids = _sorted_hex_ids(len(benchmark_questions))
    ti_id  = uuid.uuid4().hex                           # single instruction

    # Verify global uniqueness
    all_ids = sq_ids + eq_ids + bm_ids + [ti_id]
    assert len(all_ids) == len(set(all_ids)), "UUID collision — rerun the cell."

    # ---- config.sample_questions ----
    config_sq = [
        {"id": sq_ids[i], "question": [q]}
        for i, q in enumerate(sample_questions)
    ]

    # ---- data_sources.metric_views (sorted by identifier) ----
    mv_list = [
        {"identifier": k, "description": [v]}
        for k, v in sorted(metric_view_descriptions.items())
    ]

    # ---- instructions.text_instructions (exactly one entry) ----
    text_instr = [{"id": ti_id, "content": [general_instructions]}]

    # ---- instructions.example_question_sqls (sorted by id) ----
    ex_sqls = [
        {"id": eq_ids[i], "question": [q], "sql": [sql]}
        for i, (q, sql) in enumerate(example_question_sqls)
    ]

    # ---- benchmarks.questions (sorted by id) ----
    bm_list = [
        {
            "id": bm_ids[i],
            "question": [q],
            "answer": [{"format": "SQL", "content": [sql]}],
        }
        for i, (q, sql) in enumerate(benchmark_questions)
    ]

    # ---- Assemble full structure ----
    payload = {
        "version": 2,
        "config": {"sample_questions": config_sq},
        "data_sources": {"metric_views": mv_list},
        "instructions": {
            "text_instructions": text_instr,
            "example_question_sqls": ex_sqls,
        },
        "benchmarks": {"questions": bm_list},
    }

    return json.dumps(payload)


print("✅ Helper functions loaded: get_workspace_url, get_api_headers, build_serialized_space")

# COMMAND ----------

# DBTITLE 1,Create or Update Space
# =============================================================================
# CREATE OR UPDATE GENIE SPACE
# Run this cell to apply the configuration from Cells 2–7.
# =============================================================================

# ── LAYER 1: Pre-deploy gate enforcement ──
# When gate_checks is loaded, this runs comprehensive checks that
# physically block API creation of empty/incomplete Genie spaces.
if _gate_checks_loaded:
    run_genie_predeploy_gates(
        title=SPACE_TITLE,
        description=SPACE_DESCRIPTION,
        table_identifiers=TABLE_IDENTIFIERS,
        general_instructions=GENERAL_INSTRUCTIONS,
        sample_questions=SAMPLE_QUESTIONS,
        example_sqls=EXAMPLE_QUESTION_SQLS,
    )
    print("  Pre-deploy gates PASSED — proceeding to API call")

serialised = build_serialized_space(
    general_instructions=GENERAL_INSTRUCTIONS,
    metric_view_descriptions=METRIC_VIEW_DESCRIPTIONS,
    sample_questions=SAMPLE_QUESTIONS,
    example_question_sqls=EXAMPLE_QUESTION_SQLS,
    benchmark_questions=BENCHMARK_QUESTIONS,
)

# ── CRITICAL: Validate serialized_space format before API call ──
# The Genie API version 2 requires data_sources.metric_views (NOT data_sources.tables).
# Using "tables" causes "BadRequest: The zip archive contains no items" because
# the API tries to package file-based table attachments but finds none.
_ss_check = json.loads(serialised)
assert "metric_views" in _ss_check.get("data_sources", {}), (
    "SERIALIZED_SPACE FORMAT ERROR: data_sources must use 'metric_views' key, "
    "not 'tables'. The build_serialized_space function was modified incorrectly. "
    f"Found keys: {list(_ss_check.get('data_sources', {}).keys())}"
)
assert "tables" not in _ss_check.get("data_sources", {}), (
    "SERIALIZED_SPACE FORMAT ERROR: data_sources must NOT contain 'tables' key. "
    "Use 'metric_views' instead. Remove 'column_configs' from items."
)
print(f"  Serialized space validated: {len(_ss_check['data_sources']['metric_views'])} metric views, format=v2")

# --- Idempotency: find existing space by title to avoid duplicates ---
if not SPACE_ID:
    print(f"Checking for existing space with title: {SPACE_TITLE}")
    try:
        list_resp = w.api_client.do("GET", "/api/2.0/genie/spaces")
        for existing in (list_resp or {}).get("spaces", []):
            if existing.get("title") == SPACE_TITLE:
                SPACE_ID = existing["space_id"]
                print(f"  Found existing space: {SPACE_ID} — will UPDATE instead of create")
                break
    except Exception:
        pass  # List not available — proceed with CREATE

if SPACE_ID:
    # ---------- UPDATE existing space ----------
    print(f"Updating space {SPACE_ID} ...")
    result = w.api_client.do(
        "PATCH",
        f"/api/2.0/genie/spaces/{SPACE_ID}",
        headers={"Content-Type": "application/json"},
        body={
            "title": SPACE_TITLE,
            "description": SPACE_DESCRIPTION,
            "serialized_space": serialised,
        },
    )
else:
    # ---------- CREATE new space ----------
    print("Creating new space ...")
    result = w.api_client.do(
        "POST",
        "/api/2.0/genie/spaces",
        headers={"Content-Type": "application/json"},
        body={
            "title": SPACE_TITLE,
            "description": SPACE_DESCRIPTION,
            "warehouse_id": WAREHOUSE_ID,
            "table_identifiers": TABLE_IDENTIFIERS,
            "serialized_space": serialised,
        },
    )

# ---------- Report result ----------
new_id = result.get("space_id", SPACE_ID)
print(f"\n\u2705 SUCCESS")
print(f"   Space ID   : {new_id}")
print(f"   Title       : {result.get('title')}")
print(f"   Description : {result.get('description', '')[:120]}")
print(f"   Tables      : {TABLE_IDENTIFIERS}")
if not SPACE_ID:
    print(f"\n\u26a0\ufe0f  Copy this ID into Cell 2 for future updates:")
    print(f'   SPACE_ID = "{new_id}"')

# ── LAYER 2: Post-deploy API readback validation ──
# Reads the space back from the API and verifies the deployed content
# is non-empty (instructions, tables, questions all present).
if _gate_checks_loaded:
    try:
        post_deploy_result = validate_genie_from_api(new_id, SPACE_TITLE)
        print(f"\n\u2705 Post-deploy gate PASSED — API readback verified")
    except GateCheckError as e:
        print(f"\n\u274c POST-DEPLOY GATE FAILED for '{SPACE_TITLE}':")
        print(f"   {str(e)[:300]}")
        raise

# ── Compute manifest hashes for idempotency proof ──
import hashlib as _hl
_genie_source_raw = json.dumps({
    "title": SPACE_TITLE,
    "description": SPACE_DESCRIPTION,
    "table_identifiers": TABLE_IDENTIFIERS,
    "instructions_len": len(GENERAL_INSTRUCTIONS),
    "sample_questions": len(SAMPLE_QUESTIONS),
    "example_sqls": len(EXAMPLE_QUESTION_SQLS),
}, sort_keys=True)
SOURCE_HASH = _hl.sha256(_genie_source_raw.encode()).hexdigest()
READBACK_HASH = _hl.sha256(json.dumps({"space_id": new_id, "title": result.get("title", SPACE_TITLE)}, sort_keys=True).encode()).hexdigest()
print(f"  Source hash:   {SOURCE_HASH[:16]}...")
print(f"  Readback hash: {READBACK_HASH[:16]}...")

# Exit with space_id so orchestrator can validate without list API
dbutils.notebook.exit(json.dumps({"space_id": new_id, "title": result.get("title", SPACE_TITLE), "source_hash": SOURCE_HASH, "readback_hash": READBACK_HASH}))

# COMMAND ----------

# DBTITLE 1,Validate Space
# =============================================================================
# VALIDATE SPACE
# Read back the space config and display a summary.
# =============================================================================

target_id = SPACE_ID
if not target_id:
    print("No SPACE_ID set — set it in Cell 2 after creating a space.")
else:
    try:
        data = w.api_client.do(
            "GET",
            f"/api/2.0/genie/spaces/{target_id}",
            query={"include_serialized_space": "true"},
        )
        ss = json.loads(data["serialized_space"])

        sqs  = ss.get("config", {}).get("sample_questions", [])
        # NOTE: API stores metric_views under "tables" key in read-back
        mvs  = ss.get("data_sources", {}).get("metric_views", []) or \
               ss.get("data_sources", {}).get("tables", [])
        tis  = ss.get("instructions", {}).get("text_instructions", [])
        eqs  = ss.get("instructions", {}).get("example_question_sqls", [])
        bms  = ss.get("benchmarks", {}).get("questions", [])

        print("=" * 60)
        print("GENIE SPACE VALIDATION REPORT")
        print("=" * 60)
        print(f"Title        : {data.get('title')}")
        print(f"Space ID     : {data.get('space_id', target_id)}")
        print(f"Warehouse    : {data.get('warehouse_id', 'N/A')}")
        print(f"Description  : {data.get('description', '')[:120]}...")
        print()
        print(f"Metric Views        : {len(mvs)}")
        for mv in mvs:
            print(f"   • {mv['identifier'].split('.')[-1]}")
        print(f"Sample Questions    : {len(sqs)}")
        print(f"Example SQLs        : {len(eqs)}")
        print(f"Benchmark Questions : {len(bms)}")
        # Instructions are stored as multi-line arrays; join for total length
        instr_total = sum(len(''.join(t.get('content', []))) for t in tis)
        print(f"Text Instructions   : {len(tis)} block(s), {instr_total:,} chars")
        print()

        if bms:
            print("-" * 60)
            print("BENCHMARK QUESTIONS")
            print("-" * 60)
            for i, bm in enumerate(bms, 1):
                q = bm['question'][0]
                has_sql = bool(bm.get('answer'))
                marker = "\u2705" if has_sql else "\u26a0\ufe0f"
                print(f"  {i:2d}. {marker} {q}")

        # Completeness assertions (catch empty deployments)
        issues = []
        if len(sqs) < 5:
            issues.append(f"Only {len(sqs)} sample questions (need >= 5)")
        if len(eqs) < 5:
            issues.append(f"Only {len(eqs)} example SQLs (need >= 5)")
        if len(bms) < 3:
            issues.append(f"Only {len(bms)} benchmark questions (need >= 3)")
        if len(mvs) < 1:
            issues.append(f"No metric views configured (need >= 1)")
        # Instructions stored as multi-line array; join for length check
        instr_text = ''.join(tis[0].get('content', [])) if tis else ''
        if len(instr_text) < 100:
            issues.append(f"Instructions too short ({len(instr_text)} chars, need >= 100)")

        if issues:
            print("\n\u274c POST-DEPLOY VALIDATION FAILED:")
            for issue in issues:
                print(f"   \u2022 {issue}")
            raise AssertionError(
                f"Genie space deployed but INCOMPLETE. Fix config and re-run.\n"
                + "\n".join(f"  - {i}" for i in issues)
            )

        print()
        print("\u2705 Validation complete — all content checks passed")
    except Exception as e:
        if "AssertionError" in type(e).__name__:
            raise
        print(f"\u274c Failed to validate space: {e}")
        raise
