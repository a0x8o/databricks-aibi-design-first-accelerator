metric_views:
  - name: member_claims_metric_view_v10
    sql_fqn: "`aw_serverless_stable_catalog`.`aibi_member_claims`.`member_claims_metric_view_v10`"
    yaml:
      version: 1.1
      comment: Member Claims claim-detail metric view for claim volume, paid amount, denial, clean-claim, payment ratio, utilization, and provider participation KPIs.
      source: aw_serverless_stable_catalog.aibi_member_claims.fact_claim_detail_v10
      fields:
        - name: Claim ID
          expr: clm_dtl_claim_id
          comment: Claim identifier at detail-line grain.
        - name: Claim Type
          expr: clm_dtl_claim_type
          comment: Claim type such as Institutional or Professional.
        - name: Line Status
          expr: clm_dtl_line_status
          comment: Claim line adjudication/payment status.
        - name: Clean Claim Indicator
          expr: clm_dtl_clean_claim_ind
          comment: Clean claim indicator.
        - name: Place of Service
          expr: clm_dtl_place_of_service
          comment: Place of service code.
        - name: Benefit Category
          expr: clm_dtl_benefit_category
          comment: Benefit category.
        - name: Rendering Provider Specialty
          expr: clm_dtl_rendering_provider_spec
          comment: Rendering provider specialty.
        - name: Participating Provider
          expr: clm_dtl_participating_provider
          comment: Participating provider status.
        - name: Service Date
          expr: clm_dtl_specific_dos_date
          comment: Detail line date of service.
        - name: service_month
          expr: date_trunc('month', clm_dtl_specific_dos_date)
          comment: Common month dimension for dashboard filtering and time trends.
      measures:
        - name: Total Claims
          expr: COUNT(DISTINCT clm_dtl_claim_id)
          comment: Count of distinct claims.
          format:
            type: number
            decimal_places:
              type: exact
              places: 0
        - name: Total Claim Lines
          expr: COUNT(*)
          comment: Count of claim service lines.
          format:
            type: number
            decimal_places:
              type: exact
              places: 0
        - name: Total Paid Amount
          expr: SUM(clm_dtl_paid_amt)
          comment: Total paid amount across claim detail lines.
          format:
            type: currency
            currency_code: USD
            decimal_places:
              type: exact
              places: 2
        - name: Average Paid per Claim
          expr: MEASURE(`Total Paid Amount`) / NULLIF(MEASURE(`Total Claims`), 0)
          comment: Average paid amount per distinct claim.
          format:
            type: currency
            currency_code: USD
            decimal_places:
              type: exact
              places: 2
        - name: Denied Lines
          expr: SUM(CASE WHEN clm_dtl_line_status = 'Denied' THEN 1 ELSE 0 END)
          comment: Claim lines with denied status.
          format:
            type: number
            decimal_places:
              type: exact
              places: 0
        - name: Denial Rate
          expr: MEASURE(`Denied Lines`) / NULLIF(MEASURE(`Total Claim Lines`), 0)
          comment: Denied lines divided by total claim lines.
          format:
            type: percentage
            decimal_places:
              type: exact
              places: 2
        - name: Clean Lines
          expr: SUM(CASE WHEN clm_dtl_clean_claim_ind = 'Clean' THEN 1 ELSE 0 END)
          comment: Claim lines marked clean.
          format:
            type: number
            decimal_places:
              type: exact
              places: 0
        - name: Clean Claim Rate
          expr: MEASURE(`Clean Lines`) / NULLIF(MEASURE(`Total Claim Lines`), 0)
          comment: Clean lines divided by total claim lines.
          format:
            type: percentage
            decimal_places:
              type: exact
              places: 2
        - name: Total Billed Amount
          expr: SUM(clm_dtl_billed_amt)
          comment: Total billed amount across claim detail lines.
          format:
            type: currency
            currency_code: USD
            decimal_places:
              type: exact
              places: 2
        - name: Payment-to-Billed Ratio
          expr: MEASURE(`Total Paid Amount`) / NULLIF(MEASURE(`Total Billed Amount`), 0)
          comment: Total paid divided by total billed.
          format:
            type: percentage
            decimal_places:
              type: exact
              places: 2
        - name: Total Allowed Amount
          expr: SUM(clm_dtl_allowed_amt)
          comment: Total allowed amount across claim detail lines.
          format:
            type: currency
            currency_code: USD
            decimal_places:
              type: exact
              places: 2
        - name: Payment-to-Allowed Ratio
          expr: MEASURE(`Total Paid Amount`) / NULLIF(MEASURE(`Total Allowed Amount`), 0)
          comment: Total paid divided by total allowed.
          format:
            type: percentage
            decimal_places:
              type: exact
              places: 2
        - name: Unique Claim Members
          expr: COUNT(DISTINCT clm_dtl_member_nbr_sk)
          comment: Distinct member identifiers on claim detail lines.
          format:
            type: number
            decimal_places:
              type: exact
              places: 0
        - name: Average Paid per Member
          expr: MEASURE(`Total Paid Amount`) / NULLIF(MEASURE(`Unique Claim Members`), 0)
          comment: Total paid divided by unique claim member identifiers.
          format:
            type: currency
            currency_code: USD
            decimal_places:
              type: exact
              places: 2
        - name: Claims per Member
          expr: MEASURE(`Total Claims`) / NULLIF(MEASURE(`Unique Claim Members`), 0)
          comment: Distinct claims divided by unique claim members.
          format:
            type: number
            decimal_places:
              type: exact
              places: 2
        - name: Lines per Claim
          expr: MEASURE(`Total Claim Lines`) / NULLIF(MEASURE(`Total Claims`), 0)
          comment: Claim detail lines divided by distinct claims.
          format:
            type: number
            decimal_places:
              type: exact
              places: 2
        - name: Inpatient Paid Amount
          expr: SUM(CASE WHEN clm_dtl_claim_type = 'Institutional' THEN clm_dtl_paid_amt ELSE 0 END)
          comment: Paid amount for institutional claim type lines.
          format:
            type: currency
            currency_code: USD
            decimal_places:
              type: exact
              places: 2
        - name: Outpatient Paid Amount
          expr: SUM(CASE WHEN clm_dtl_claim_type = 'Professional' THEN clm_dtl_paid_amt ELSE 0 END)
          comment: Paid amount for professional claim type lines.
          format:
            type: currency
            currency_code: USD
            decimal_places:
              type: exact
              places: 2
        - name: Participating Provider Paid Amount
          expr: SUM(CASE WHEN clm_dtl_participating_provider = 'Participating' THEN clm_dtl_paid_amt ELSE 0 END)
          comment: Paid amount for participating provider claim lines.
          format:
            type: currency
            currency_code: USD
            decimal_places:
              type: exact
              places: 2
        - name: Participating Provider Rate
          expr: MEASURE(`Participating Provider Paid Amount`) / NULLIF(MEASURE(`Total Paid Amount`), 0)
          comment: Participating provider paid amount divided by total paid amount.
          format:
            type: percentage
            decimal_places:
              type: exact
              places: 2
  - name: member_claims_enrollment_metric_view_v10
    sql_fqn: "`aw_serverless_stable_catalog`.`aibi_member_claims`.`member_claims_enrollment_metric_view_v10`"
    yaml:
      version: 1.1
      comment: Member Claims enrollment metric view for active members, new enrollment, and geographic distribution at enrollment grain.
      source: aw_serverless_stable_catalog.aibi_member_claims.fact_member_enrollment_v10
      fields:
        - name: Enrollment Status
          expr: source.mbr_enr_status
          comment: Enrollment status.
        - name: Line of Business
          expr: source.mbr_enr_line_of_business
          comment: Enrollment line of business.
        - name: Plan ID
          expr: source.mbr_enr_plan_id
          comment: Enrollment plan identifier.
        - name: Product ID
          expr: source.mbr_enr_product_id
          comment: Enrollment product identifier.
        - name: Member State
          expr: member.mbr_state
          comment: Member state from member dimension.
        - name: Member ZIP Code
          expr: member.mbr_zip_code
          comment: Member ZIP code from member dimension.
        - name: Effective Date
          expr: source.mbr_enr_effective_date
          comment: Enrollment effective date.
        - name: service_month
          expr: date_trunc('month', source.mbr_enr_effective_date)
          comment: Common month dimension for dashboard filtering and time trends.
      measures:
        - name: New Member Enrollment
          expr: COUNT(DISTINCT CASE WHEN source.mbr_enr_effective_date IS NOT NULL THEN source.member_sk END)
          comment: Count of distinct members with an enrollment effective date in the current query period.
          format:
            type: number
            decimal_places:
              type: exact
              places: 0
        - name: Active Members
          expr: COUNT(DISTINCT source.member_sk)
          comment: Distinct active/enrolled members represented by enrollment records.
          format:
            type: number
            decimal_places:
              type: exact
              places: 0
        - name: Members by Geography
          expr: COUNT(DISTINCT source.member_sk)
          comment: Distinct members sliced by member geography dimensions.
          format:
            type: number
            decimal_places:
              type: exact
              places: 0
        - name: Enrollment Records
          expr: COUNT(*)
          comment: Count of enrollment records.
          format:
            type: number
            decimal_places:
              type: exact
              places: 0
      joins:
        - name: member
          source: aw_serverless_stable_catalog.aibi_member_claims.dim_member_v10
          'on': source.member_sk = member.member_sk
          rely:
            at_most_one_match: true
