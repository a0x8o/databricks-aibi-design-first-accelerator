metric_views:
  - name: member_claims_metric_view_v7
    catalog: aw_serverless_stable_catalog
    schema: aibi_member_claims
    sql_fqn: aw_serverless_stable_catalog.aibi_member_claims.member_claims_metric_view_v7
    yaml:
      version: 1.1
      comment: Claim-line grain metric view for member claims financial, operational, utilization, and provider KPIs.
      source: aw_serverless_stable_catalog.aibi_member_claims.fact_claim_detail_v7
      fields:
        - name: Service Date
          expr: clm_dtl_specific_dos_date
          comment: Claim detail date of service.
        - name: service_month
          expr: DATE_TRUNC('MONTH', clm_dtl_specific_dos_date)
          comment: Month of service, common date dimension across member claims metric views.
        - name: claim_type
          expr: clm_dtl_claim_type
          comment: Claim type on the claim detail line.
        - name: line_status
          expr: clm_dtl_line_status
          comment: Claim line status.
        - name: clean_claim_indicator
          expr: clm_dtl_clean_claim_ind
          comment: Clean-claim indicator for the claim line.
        - name: benefit_category
          expr: clm_dtl_benefit_category
          comment: Benefit category.
        - name: benefit_level
          expr: clm_dtl_benefit_level
          comment: Benefit level.
        - name: place_of_service
          expr: clm_dtl_place_of_service
          comment: Place of service code.
        - name: rendering_provider_type
          expr: clm_dtl_rendering_provider_type
          comment: Rendering provider type.
        - name: rendering_provider_spec
          expr: clm_dtl_rendering_provider_spec
          comment: Rendering provider specialty.
        - name: participating_provider
          expr: clm_dtl_participating_provider
          comment: Participating provider indicator/category.
        - name: adjudication_status
          expr: clm_dtl_adjudication_status
          comment: Adjudication status.
        - name: procedure_code
          expr: clm_dtl_procedure_code
          comment: Procedure code.
        - name: revenue_code
          expr: clm_dtl_revenue_code
          comment: Revenue code.
      measures:
        - name: Total Claims
          expr: COUNT(DISTINCT clm_dtl_claim_id)
          comment: Count of distinct claims represented by claim detail lines.
          format:
            type: number
            decimal_places: {type: exact, places: 0}
        - name: Total Claim Lines
          expr: COUNT(*)
          comment: Count of claim service lines.
          format:
            type: number
            decimal_places: {type: exact, places: 0}
        - name: Total Paid Amount
          expr: SUM(clm_dtl_paid_amt)
          comment: Sum of paid amount across claim detail lines.
          format:
            type: currency
            currency_code: USD
            decimal_places: {type: exact, places: 2}
        - name: Average Paid per Claim
          expr: MEASURE(`Total Paid Amount`) / NULLIF(MEASURE(`Total Claims`), 0)
          comment: Total paid amount divided by distinct claims.
          format:
            type: currency
            currency_code: USD
            decimal_places: {type: exact, places: 2}
        - name: Denied Lines
          expr: SUM(CASE WHEN clm_dtl_line_status = 'Denied' THEN 1 ELSE 0 END)
          comment: Count of denied claim lines.
          format:
            type: number
            decimal_places: {type: exact, places: 0}
        - name: Denial Rate
          expr: MEASURE(`Denied Lines`) / NULLIF(MEASURE(`Total Claim Lines`), 0)
          comment: Denied claim lines divided by total claim lines.
          format:
            type: percentage
            decimal_places: {type: exact, places: 2}
        - name: Clean Lines
          expr: SUM(CASE WHEN clm_dtl_clean_claim_ind = 'Clean' THEN 1 ELSE 0 END)
          comment: Count of clean claim lines.
          format:
            type: number
            decimal_places: {type: exact, places: 0}
        - name: Clean Claim Rate
          expr: MEASURE(`Clean Lines`) / NULLIF(MEASURE(`Total Claim Lines`), 0)
          comment: Clean claim lines divided by total claim lines.
          format:
            type: percentage
            decimal_places: {type: exact, places: 2}
        - name: Total Billed Amount
          expr: SUM(clm_dtl_billed_amt)
          comment: Sum of billed amount across claim detail lines.
          format:
            type: currency
            currency_code: USD
            decimal_places: {type: exact, places: 2}
        - name: Payment-to-Billed Ratio
          expr: MEASURE(`Total Paid Amount`) / NULLIF(MEASURE(`Total Billed Amount`), 0)
          comment: Paid amount divided by billed amount.
          format:
            type: percentage
            decimal_places: {type: exact, places: 2}
        - name: Total Allowed Amount
          expr: SUM(clm_dtl_allowed_amt)
          comment: Sum of allowed amount across claim detail lines.
          format:
            type: currency
            currency_code: USD
            decimal_places: {type: exact, places: 2}
        - name: Payment-to-Allowed Ratio
          expr: MEASURE(`Total Paid Amount`) / NULLIF(MEASURE(`Total Allowed Amount`), 0)
          comment: Paid amount divided by allowed amount.
          format:
            type: percentage
            decimal_places: {type: exact, places: 2}
        - name: Unique Claim Members
          expr: COUNT(DISTINCT clm_dtl_member_nbr_sk)
          comment: Count of unique members represented on claim detail lines.
          format:
            type: number
            decimal_places: {type: exact, places: 0}
        - name: Average Paid per Member
          expr: MEASURE(`Total Paid Amount`) / NULLIF(MEASURE(`Unique Claim Members`), 0)
          comment: Paid amount divided by unique claim members.
          format:
            type: currency
            currency_code: USD
            decimal_places: {type: exact, places: 2}
        - name: Claims per Member
          expr: MEASURE(`Total Claims`) / NULLIF(MEASURE(`Unique Claim Members`), 0)
          comment: Distinct claims divided by unique claim members.
          format:
            type: number
            decimal_places: {type: exact, places: 2}
        - name: Lines per Claim
          expr: MEASURE(`Total Claim Lines`) / NULLIF(MEASURE(`Total Claims`), 0)
          comment: Claim service lines divided by distinct claims.
          format:
            type: number
            decimal_places: {type: exact, places: 2}
        - name: Inpatient Paid Amount
          expr: SUM(CASE WHEN clm_dtl_claim_type = 'Institutional' THEN clm_dtl_paid_amt ELSE 0 END)
          comment: Paid amount for Institutional claim type lines.
          format:
            type: currency
            currency_code: USD
            decimal_places: {type: exact, places: 2}
        - name: Outpatient Paid Amount
          expr: SUM(CASE WHEN clm_dtl_claim_type = 'Professional' THEN clm_dtl_paid_amt ELSE 0 END)
          comment: Paid amount for Professional claim type lines.
          format:
            type: currency
            currency_code: USD
            decimal_places: {type: exact, places: 2}
        - name: Participating Paid Amount
          expr: SUM(CASE WHEN clm_dtl_participating_provider IN ('Participating', 'Tier 1 Preferred') THEN clm_dtl_paid_amt ELSE 0 END)
          comment: Paid amount for participating provider categories.
          format:
            type: currency
            currency_code: USD
            decimal_places: {type: exact, places: 2}
        - name: Participating Provider Rate
          expr: MEASURE(`Participating Paid Amount`) / NULLIF(MEASURE(`Total Paid Amount`), 0)
          comment: Participating provider paid amount divided by total paid amount.
          format:
            type: percentage
            decimal_places: {type: exact, places: 2}
  - name: member_claims_enrollment_metric_view_v7
    catalog: aw_serverless_stable_catalog
    schema: aibi_member_claims
    sql_fqn: aw_serverless_stable_catalog.aibi_member_claims.member_claims_enrollment_metric_view_v7
    yaml:
      version: 1.1
      comment: Enrollment grain metric view for active members, new enrollment, line-of-business, and geography KPIs.
      source: aw_serverless_stable_catalog.aibi_member_claims.fact_member_enrollment_enriched_v7
      fields:
        - name: Service Date
          expr: mbr_enr_effective_date
          comment: Enrollment effective date used as common service date for enrollment KPIs.
        - name: service_month
          expr: DATE_TRUNC('MONTH', mbr_enr_effective_date)
          comment: Enrollment effective month, common date dimension across member claims metric views.
        - name: line_of_business
          expr: mbr_enr_line_of_business
          comment: Enrollment line of business.
        - name: enrollment_status
          expr: mbr_enr_status
          comment: Enrollment status.
        - name: member_state
          expr: mbr_state
          comment: Member state from current member dimension.
        - name: member_zip_code
          expr: mbr_zip_code
          comment: Member ZIP code from current member dimension.
        - name: member_sex
          expr: mbr_sex
          comment: Member sex.
        - name: member_race
          expr: mbr_race
          comment: Member race.
        - name: group_name
          expr: mbr_enr_group_name
          comment: Enrollment group name.
        - name: subgroup_name
          expr: mbr_enr_subgroup_name
          comment: Enrollment subgroup name.
        - name: product_id
          expr: mbr_enr_product_id
          comment: Enrollment product identifier.
      measures:
        - name: Active Members
          expr: COUNT(DISTINCT member_sk)
          comment: Distinct active or enrolled members represented by enrollment records.
          format:
            type: number
            decimal_places: {type: exact, places: 0}
        - name: New Member Enrollment
          expr: COUNT(DISTINCT member_sk)
          comment: Distinct members entering coverage in the selected effective-date period.
          format:
            type: number
            decimal_places: {type: exact, places: 0}
  - name: member_claims_monthly_metric_view_v7
    catalog: aw_serverless_stable_catalog
    schema: aibi_member_claims
    sql_fqn: aw_serverless_stable_catalog.aibi_member_claims.member_claims_monthly_metric_view_v7
    yaml:
      version: 1.1
      comment: Monthly pre-aggregated metric view for cross-grain member-claim ratio KPIs.
      source: aw_serverless_stable_catalog.aibi_member_claims.member_claims_monthly_summary_v7
      fields:
        - name: service_month
          expr: service_month
          comment: Month shared by pre-aggregated claims and enrollment components.
      measures:
        - name: Monthly Paid Amount
          expr: SUM(total_paid_amount)
          comment: Monthly paid amount from claim line pre-aggregation.
          format:
            type: currency
            currency_code: USD
            decimal_places: {type: exact, places: 2}
        - name: Member Months
          expr: SUM(member_months)
          comment: Sum of monthly member-month exposure.
          format:
            type: number
            decimal_places: {type: exact, places: 0}
        - name: PMPM
          expr: MEASURE(`Monthly Paid Amount`) / NULLIF(MEASURE(`Member Months`), 0)
          comment: Paid amount per member month.
          format:
            type: currency
            currency_code: USD
            decimal_places: {type: exact, places: 2}
        - name: Monthly Claims
          expr: SUM(total_claims)
          comment: Monthly distinct claims from claim line pre-aggregation.
          format:
            type: number
            decimal_places: {type: exact, places: 0}
        - name: Claims per 1000 Members
          expr: (MEASURE(`Monthly Claims`) / NULLIF(MEASURE(`Member Months`), 0)) * 1000
          comment: Distinct claims per 1,000 member months.
          format:
            type: number
            decimal_places: {type: exact, places: 1}
        - name: Members With Claims
          expr: SUM(members_with_claims)
          comment: Monthly members represented on claims.
          format:
            type: number
            decimal_places: {type: exact, places: 0}
        - name: Active Enrolled Members
          expr: SUM(active_enrolled_members)
          comment: Monthly active enrolled members.
          format:
            type: number
            decimal_places: {type: exact, places: 0}
        - name: Utilization Rate
          expr: MEASURE(`Members With Claims`) / NULLIF(MEASURE(`Active Enrolled Members`), 0)
          comment: Members with claims divided by active enrolled members.
          format:
            type: percentage
            decimal_places: {type: exact, places: 2}
