metric_views:
  - name: member_claims_metric_view_v4
    sql_fqn: aw_serverless_stable_catalog.aibi_member_claims.member_claims_metric_view_v4
    yaml:
      version: 1.1
      comment: Member Claims claim-line metric view for claims, paid amounts, denial, clean-claim, normalized member-claim, and provider participation KPIs.
      source: aw_serverless_stable_catalog.aibi_member_claims.fact_claim_detail_v4
      fields:
        - name: service_date
          expr: clm_dtl_specific_dos_date
          comment: Claim line service date.
        - name: service_month
          expr: DATE_TRUNC('MONTH', clm_dtl_specific_dos_date)
          comment: Month of claim line service date for common dashboard filtering.
        - name: claim_type
          expr: clm_dtl_claim_type
          comment: Claim type from claim detail.
        - name: benefit_category
          expr: clm_dtl_benefit_category
          comment: Benefit category from claim detail.
        - name: benefit_level
          expr: clm_dtl_benefit_level
          comment: Benefit level from claim detail.
        - name: line_status
          expr: clm_dtl_line_status
          comment: Claim detail line status.
        - name: clean_claim_indicator
          expr: clm_dtl_clean_claim_ind
          comment: Clean claim indicator.
        - name: provider_participation
          expr: clm_dtl_participating_provider
          comment: Participating provider indicator/category.
        - name: place_of_service
          expr: clm_dtl_place_of_service
          comment: Place of service code.
        - name: procedure_code
          expr: clm_dtl_procedure_code
          comment: Procedure code.
        - name: rendering_provider_type
          expr: clm_dtl_rendering_provider_type
          comment: Rendering provider type.
        - name: rendering_provider_specialty
          expr: clm_dtl_rendering_provider_spec
          comment: Rendering provider specialty.
      measures:
        - name: Total Claims
          expr: COUNT(DISTINCT clm_dtl_claim_id)
          comment: Count of distinct claims represented by claim detail lines.
          format:
            type: number
            decimal_places: {type: exact, places: 0}
        - name: Total Claim Lines
          expr: COUNT(*)
          comment: Count of claim detail lines.
          format:
            type: number
            decimal_places: {type: exact, places: 0}
        - name: Total Paid Amount
          expr: SUM(clm_dtl_paid_amt)
          comment: Sum of paid amount across claim detail lines.
          format:
            type: currency
            currency_code: USD
            decimal_places: {type: exact, places: 2}
        - name: Total Billed Amount
          expr: SUM(clm_dtl_billed_amt)
          comment: Sum of billed amount across claim detail lines.
          format:
            type: currency
            currency_code: USD
            decimal_places: {type: exact, places: 2}
        - name: Total Allowed Amount
          expr: SUM(clm_dtl_allowed_amt)
          comment: Sum of allowed amount across claim detail lines.
          format:
            type: currency
            currency_code: USD
            decimal_places: {type: exact, places: 2}
        - name: Unique Claim Members
          expr: COUNT(DISTINCT clm_dtl_member_nbr_sk)
          comment: Count of distinct claim member keys observed in claim detail.
          format:
            type: number
            decimal_places: {type: exact, places: 0}
        - name: Claim Member Months
          expr: COUNT(DISTINCT clm_dtl_member_nbr_sk, DATE_TRUNC('MONTH', clm_dtl_specific_dos_date))
          comment: Distinct claim-observed member-months for normalized claim KPIs.
          format:
            type: number
            decimal_places: {type: exact, places: 0}
        - name: Denied Lines
          expr: SUM(CASE WHEN clm_dtl_line_status = 'Denied' THEN 1 ELSE 0 END)
          comment: Count of denied claim detail lines.
          format:
            type: number
            decimal_places: {type: exact, places: 0}
        - name: Clean Lines
          expr: SUM(CASE WHEN clm_dtl_clean_claim_ind = 'Clean' THEN 1 ELSE 0 END)
          comment: Count of clean claim detail lines.
          format:
            type: number
            decimal_places: {type: exact, places: 0}
        - name: Participating Provider Paid Amount
          expr: SUM(CASE WHEN clm_dtl_participating_provider = 'Participating' THEN clm_dtl_paid_amt ELSE 0 END)
          comment: Paid amount attributed to participating providers.
          format:
            type: currency
            currency_code: USD
            decimal_places: {type: exact, places: 2}
        - name: Inpatient Paid Amount
          expr: SUM(CASE WHEN clm_dtl_claim_type = 'Institutional' THEN clm_dtl_paid_amt ELSE 0 END)
          comment: Paid amount for institutional/inpatient-style claims.
          format:
            type: currency
            currency_code: USD
            decimal_places: {type: exact, places: 2}
        - name: Outpatient Paid Amount
          expr: SUM(CASE WHEN clm_dtl_claim_type = 'Professional' THEN clm_dtl_paid_amt ELSE 0 END)
          comment: Paid amount for professional/outpatient-style claims.
          format:
            type: currency
            currency_code: USD
            decimal_places: {type: exact, places: 2}
        - name: Average Paid per Claim
          expr: MEASURE(`Total Paid Amount`) / NULLIF(MEASURE(`Total Claims`), 0)
          comment: Paid amount divided by distinct claim count.
          format:
            type: currency
            currency_code: USD
            decimal_places: {type: exact, places: 2}
        - name: PMPM
          expr: MEASURE(`Total Paid Amount`) / NULLIF(MEASURE(`Claim Member Months`), 0)
          comment: Paid amount divided by claim-observed member-month exposure.
          format:
            type: currency
            currency_code: USD
            decimal_places: {type: exact, places: 2}
        - name: Claims per 1,000 Members
          expr: (MEASURE(`Total Claims`) / NULLIF(MEASURE(`Claim Member Months`), 0)) * 1000
          comment: Distinct claims per 1,000 claim-observed member-months.
          format:
            type: number
            decimal_places: {type: exact, places: 1}
        - name: Denial Rate
          expr: MEASURE(`Denied Lines`) / NULLIF(MEASURE(`Total Claim Lines`), 0)
          comment: Denied lines divided by total claim lines.
          format:
            type: percentage
            decimal_places: {type: exact, places: 2}
        - name: Clean Claim Rate
          expr: MEASURE(`Clean Lines`) / NULLIF(MEASURE(`Total Claim Lines`), 0)
          comment: Clean lines divided by total claim lines.
          format:
            type: percentage
            decimal_places: {type: exact, places: 2}
        - name: Payment-to-Billed Ratio
          expr: MEASURE(`Total Paid Amount`) / NULLIF(MEASURE(`Total Billed Amount`), 0)
          comment: Paid amount divided by billed amount.
          format:
            type: percentage
            decimal_places: {type: exact, places: 2}
        - name: Payment-to-Allowed Ratio
          expr: MEASURE(`Total Paid Amount`) / NULLIF(MEASURE(`Total Allowed Amount`), 0)
          comment: Paid amount divided by allowed amount.
          format:
            type: percentage
            decimal_places: {type: exact, places: 2}
        - name: Average Paid per Member
          expr: MEASURE(`Total Paid Amount`) / NULLIF(MEASURE(`Unique Claim Members`), 0)
          comment: Paid amount divided by distinct members with claims.
          format:
            type: currency
            currency_code: USD
            decimal_places: {type: exact, places: 2}
        - name: Claims per Member
          expr: MEASURE(`Total Claims`) / NULLIF(MEASURE(`Unique Claim Members`), 0)
          comment: Distinct claims divided by distinct members with claims.
          format:
            type: number
            decimal_places: {type: exact, places: 2}
        - name: Lines per Claim
          expr: MEASURE(`Total Claim Lines`) / NULLIF(MEASURE(`Total Claims`), 0)
          comment: Claim detail lines divided by distinct claims.
          format:
            type: number
            decimal_places: {type: exact, places: 2}
        - name: Participating Provider Rate
          expr: MEASURE(`Participating Provider Paid Amount`) / NULLIF(MEASURE(`Total Paid Amount`), 0)
          comment: Participating provider paid amount divided by total paid amount.
          format:
            type: percentage
            decimal_places: {type: exact, places: 2}
  - name: member_claims_enrollment_metric_view_v4
    sql_fqn: aw_serverless_stable_catalog.aibi_member_claims.member_claims_enrollment_metric_view_v4
    yaml:
      version: 1.1
      comment: Member Claims enrollment metric view for active members and new member enrollment by line of business.
      source: aw_serverless_stable_catalog.aibi_member_claims.fact_member_enrollment_v4
      fields:
        - name: service_date
          expr: mbr_enr_effective_date
          comment: Enrollment effective date.
        - name: service_month
          expr: DATE_TRUNC('MONTH', mbr_enr_effective_date)
          comment: Enrollment effective month for common dashboard filtering.
        - name: line_of_business
          expr: mbr_enr_line_of_business
          comment: Enrollment line of business.
        - name: enrollment_status
          expr: mbr_enr_status
          comment: Enrollment status.
        - name: plan_id
          expr: mbr_enr_plan_id
          comment: Enrollment plan identifier.
        - name: product_id
          expr: mbr_enr_product_id
          comment: Enrollment product identifier.
        - name: termination_reason
          expr: mbr_enr_termination_reason
          comment: Enrollment termination reason.
      measures:
        - name: New Member Enrollment
          expr: COUNT(DISTINCT member_sk)
          comment: Count of distinct members entering coverage in the selected period.
          format:
            type: number
            decimal_places: {type: exact, places: 0}
        - name: Active Members
          expr: COUNT(DISTINCT CASE WHEN is_active = true THEN member_sk ELSE NULL END)
          comment: Count of distinct active enrolled members.
          format:
            type: number
            decimal_places: {type: exact, places: 0}
        - name: Enrollment Records
          expr: COUNT(*)
          comment: Count of enrollment records.
          format:
            type: number
            decimal_places: {type: exact, places: 0}
  - name: member_claims_member_metric_view_v4
    sql_fqn: aw_serverless_stable_catalog.aibi_member_claims.member_claims_member_metric_view_v4
    yaml:
      version: 1.1
      comment: Member Claims member dimension metric view for active members by geography and demographics.
      source: aw_serverless_stable_catalog.aibi_member_claims.dim_member_v4
      fields:
        - name: service_date
          expr: mbr_extract_date
          comment: Member extract date used as member snapshot date.
        - name: service_month
          expr: DATE_TRUNC('MONTH', mbr_extract_date)
          comment: Member extract month for common dashboard filtering.
        - name: state
          expr: mbr_state
          comment: Member state.
        - name: zip_code
          expr: mbr_zip_code
          comment: Member ZIP code.
        - name: line_of_business
          expr: mbr_line_of_business
          comment: Member line of business.
        - name: sex
          expr: mbr_sex
          comment: Member sex.
        - name: race
          expr: mbr_race
          comment: Member race.
        - name: ethnicity
          expr: mbr_ethnicity
          comment: Member ethnicity.
      measures:
        - name: Active Members by Geography
          expr: COUNT(DISTINCT CASE WHEN is_active = true THEN member_sk ELSE NULL END)
          comment: Count of distinct active members by geography and demographics.
          format:
            type: number
            decimal_places: {type: exact, places: 0}
