metric_view_plan:
  total_kpis: 23
  implementable_in_metric_views: 19
  not_implemented: 3
  skipped: 1
  max_metric_views_per_domain: 4
  metric_views:
    - name: member_claims_metric_view_v4
      sql_fqn: aw_serverless_stable_catalog.aibi_member_claims.member_claims_metric_view_v4
      source: fact_claim_detail_v4
      source_fqn: aw_serverless_stable_catalog.aibi_member_claims.fact_claim_detail_v4
      primary: true
      intermediate_view: null
      grain: one row represents one claim detail line
      common_time_dimension: service_month
      kpis: [C-1, C-2, C-3, C-4, MC-1, MC-2, ADD-1, ADD-2, ADD-3, ADD-4, ADD-5, ADD-6, ADD-7, ADD-8, ADD-9, ADD-10]
    - name: member_claims_enrollment_metric_view_v4
      sql_fqn: aw_serverless_stable_catalog.aibi_member_claims.member_claims_enrollment_metric_view_v4
      source: fact_member_enrollment_v4
      source_fqn: aw_serverless_stable_catalog.aibi_member_claims.fact_member_enrollment_v4
      primary: false
      intermediate_view: null
      grain: one row represents one member enrollment event or enrollment period
      common_time_dimension: service_month
      kpis: [M-1, M-2]
    - name: member_claims_member_metric_view_v4
      sql_fqn: aw_serverless_stable_catalog.aibi_member_claims.member_claims_member_metric_view_v4
      source: dim_member_v4
      source_fqn: aw_serverless_stable_catalog.aibi_member_claims.dim_member_v4
      primary: false
      intermediate_view: null
      grain: one row represents one current member dimension record
      common_time_dimension: service_month
      kpis: [M-3]
  grain_analysis_gate_4_5:
    status: PASS
    fact_tables_in_schema: [fact_member_enrollment_v4, fact_claim_header_v4, fact_claim_detail_v4]
    fact_tables_with_ready_kpis: [fact_member_enrollment_v4, fact_claim_detail_v4]
    planned_sources_for_ready_fact_grains: [fact_member_enrollment_v4, fact_claim_detail_v4]
    notes:
      - fact_claim_header_v4 has no READY KPI requiring header-grain metric view.
      - dim_member_v4 has READY member geography KPI and is planned as a dimension-sourced metric view to avoid unsafe one-to-many address joins.
      - A single KPI at a distinct source grain is retained per metric view guardrails.
  not_implemented:
    - kpi: MC-4
      name: High-Cost Member Count
      reason: Requires member-level pre-aggregation and HAVING threshold; metric view measure cannot filter on aggregated member spend within the same measure.
      status: NOT_IMPLEMENTED
      documentation_only: true
      sql: |
        SELECT COUNT(*) AS high_cost_member_count
        FROM (
          SELECT clm_dtl_member_nbr_sk, SUM(clm_dtl_paid_amt) AS member_paid
          FROM aw_serverless_stable_catalog.aibi_member_claims.fact_claim_detail_v4
          GROUP BY clm_dtl_member_nbr_sk
          HAVING SUM(clm_dtl_paid_amt) > 10000
        ) s;
      validated: true
      validation_row_count: 13
      manual_implementation_notes: |
        To implement: Add as a named SQL dataset in the dashboard. Cannot be a metric view measure because it requires HAVING over member-level pre-aggregation.
    - kpi: W-1
      name: Rolling 3-Month PMPM
      reason: Requires rolling window over independently aggregated numerator and denominator; implemented as dashboard/reference SQL rather than metric view measure for deterministic semantics.
      status: NOT_IMPLEMENTED
      documentation_only: true
      sql: |
        SELECT service_month,
               SUM(monthly_paid) OVER (ORDER BY service_month ROWS BETWEEN 2 PRECEDING AND CURRENT ROW)
               / NULLIF(SUM(member_months) OVER (ORDER BY service_month ROWS BETWEEN 2 PRECEDING AND CURRENT ROW), 0) AS rolling_3m_pmpm
        FROM (
          SELECT DATE_TRUNC('MONTH', clm_dtl_specific_dos_date) AS service_month,
                 SUM(clm_dtl_paid_amt) AS monthly_paid,
                 COUNT(DISTINCT CONCAT(clm_dtl_member_nbr_sk, '-', DATE_FORMAT(clm_dtl_specific_dos_date, 'yyyy-MM'))) AS member_months
          FROM aw_serverless_stable_catalog.aibi_member_claims.fact_claim_detail_v4
          GROUP BY DATE_TRUNC('MONTH', clm_dtl_specific_dos_date)
        ) m;
      validated: true
      validation_row_count: 60
      manual_implementation_notes: |
        To implement: Add as a named SQL dataset in the dashboard. Cannot be safely represented as a simple metric view measure because numerator and denominator require synchronized rolling windows.
    - kpi: W-2
      name: MoM Active Member Growth
      reason: Requires prior-period LAG over monthly active member counts; documented as reference SQL.
      status: NOT_IMPLEMENTED
      documentation_only: true
      sql: |
        SELECT service_month,
               (active_members - LAG(active_members) OVER (ORDER BY service_month))
               / NULLIF(LAG(active_members) OVER (ORDER BY service_month), 0) AS mom_active_member_growth
        FROM (
          SELECT DATE_TRUNC('MONTH', mbr_enr_effective_date) AS service_month,
                 COUNT(DISTINCT member_sk) AS active_members
          FROM aw_serverless_stable_catalog.aibi_member_claims.fact_member_enrollment_v4
          WHERE is_active = true
          GROUP BY DATE_TRUNC('MONTH', mbr_enr_effective_date)
        ) m;
      validated: true
      validation_row_count: 27
      manual_implementation_notes: |
        To implement: Add as a named SQL dataset in the dashboard. Cannot be a metric view measure because it requires LAG/prior-period offset.
  skipped_kpis:
    - kpi: MC-3
      name: Utilization Rate
      status: SKIPPED_UNSAFE_GRAIN
      reason: Requires claims numerator and enrollment denominator across different fact grains and incompatible member key datatypes (claim detail member key is varchar; enrollment member_sk is bigint). No safe common metric view source without an upstream conformed key/intermediate pre-aggregation.
