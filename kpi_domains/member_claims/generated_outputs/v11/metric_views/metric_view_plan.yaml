metric_view_plan:
  total_kpis: 23
  implementable_in_metric_views: 17
  not_implemented: 6
  metric_views:
    - name: member_claims_metric_view_v11
      sql_fqn: aw_serverless_stable_catalog.aibi_member_claims.member_claims_metric_view_v11
      source: fact_claim_detail_v11
      primary: true
      intermediate_view: null
      grain: one claim detail/service line row per claim id and line number
      kpis: [C-1, C-2, C-3, C-4, DER-1, DER-2, DER-3, DER-4, DER-5, DER-6, DER-7, DER-8, DER-9, DER-10]
    - name: member_claims_enrollment_metric_view_v11
      sql_fqn: aw_serverless_stable_catalog.aibi_member_claims.member_claims_enrollment_metric_view_v11
      source: fact_member_enrollment_v11
      primary: false
      intermediate_view: null
      grain: one member enrollment row per enrollment_sk
      kpis: [M-1, M-2, M-3]
  not_implemented:
    - kpi: MC-1
      name: PMPM
      reason: Requires numerator from claim detail fact and denominator from enrollment fact at monthly aggregate grain; no safe single source metric view can preserve both grains without pre-aggregate SQL.
      status: NOT_IMPLEMENTED
      documentation_only: true
      sql: |
        SELECT COALESCE(paid.service_month, enr.service_month) AS service_month, paid.total_paid / NULLIF(enr.member_months, 0) AS pmpm
        FROM (SELECT DATE_TRUNC('MONTH', clm_dtl_specific_dos_date) AS service_month, SUM(clm_dtl_paid_amt) AS total_paid FROM aw_serverless_stable_catalog.aibi_member_claims.fact_claim_detail_v11 GROUP BY DATE_TRUNC('MONTH', clm_dtl_specific_dos_date)) paid
        FULL OUTER JOIN (SELECT DATE_TRUNC('MONTH', mbr_enr_effective_date) AS service_month, COUNT(DISTINCT CONCAT(CAST(member_sk AS STRING), '-', CAST(DATE_TRUNC('MONTH', mbr_enr_effective_date) AS STRING))) AS member_months FROM aw_serverless_stable_catalog.aibi_member_claims.fact_member_enrollment_v11 GROUP BY DATE_TRUNC('MONTH', mbr_enr_effective_date)) enr
        ON paid.service_month = enr.service_month
      validated: true
      validation_row_count: 60
      manual_implementation_notes: Add as a named SQL dataset in the dashboard. Cannot be a metric view measure due to cross-fact aggregate grain.
    - kpi: MC-2
      name: Claims per 1,000 Members
      reason: Requires claim fact numerator and enrollment fact member-month denominator at different source grains.
      status: NOT_IMPLEMENTED
      documentation_only: true
      sql: |
        SELECT COALESCE(claims.service_month, enr.service_month) AS service_month, (claims.total_claims / NULLIF(enr.member_months, 0)) * 1000 AS claims_per_1000
        FROM (SELECT DATE_TRUNC('MONTH', clm_dtl_specific_dos_date) AS service_month, COUNT(DISTINCT clm_dtl_claim_id) AS total_claims FROM aw_serverless_stable_catalog.aibi_member_claims.fact_claim_detail_v11 GROUP BY DATE_TRUNC('MONTH', clm_dtl_specific_dos_date)) claims
        FULL OUTER JOIN (SELECT DATE_TRUNC('MONTH', mbr_enr_effective_date) AS service_month, COUNT(DISTINCT CONCAT(CAST(member_sk AS STRING), '-', CAST(DATE_TRUNC('MONTH', mbr_enr_effective_date) AS STRING))) AS member_months FROM aw_serverless_stable_catalog.aibi_member_claims.fact_member_enrollment_v11 GROUP BY DATE_TRUNC('MONTH', mbr_enr_effective_date)) enr
        ON claims.service_month = enr.service_month
      validated: true
      validation_row_count: 60
      manual_implementation_notes: Add as a named SQL dataset in the dashboard. Cannot be a metric view measure due to incompatible fact grains.
    - kpi: MC-3
      name: Utilization Rate
      reason: Requires distinct members with claims from claim detail fact divided by active enrolled members from enrollment fact.
      status: NOT_IMPLEMENTED
      documentation_only: true
      sql: |
        SELECT COALESCE(claims.service_month, enr.service_month) AS service_month, claims.members_with_claims / NULLIF(enr.active_enrolled_members, 0) AS utilization_rate
        FROM (SELECT DATE_TRUNC('MONTH', clm_dtl_specific_dos_date) AS service_month, COUNT(DISTINCT clm_dtl_member_nbr_sk) AS members_with_claims FROM aw_serverless_stable_catalog.aibi_member_claims.fact_claim_detail_v11 GROUP BY DATE_TRUNC('MONTH', clm_dtl_specific_dos_date)) claims
        FULL OUTER JOIN (SELECT DATE_TRUNC('MONTH', mbr_enr_effective_date) AS service_month, COUNT(DISTINCT member_sk) AS active_enrolled_members FROM aw_serverless_stable_catalog.aibi_member_claims.fact_member_enrollment_v11 WHERE is_active = true GROUP BY DATE_TRUNC('MONTH', mbr_enr_effective_date)) enr
        ON claims.service_month = enr.service_month
      validated: true
      validation_row_count: 60
      manual_implementation_notes: Add as a named SQL dataset in the dashboard. Cannot be a metric view measure due to cross-fact numerator/denominator.
    - kpi: MC-4
      name: High-Cost Member Count
      reason: Requires HAVING SUM(paid) > threshold after member-level pre-aggregation.
      status: NOT_IMPLEMENTED
      documentation_only: true
      sql: |
        SELECT COUNT(*) AS high_cost_member_count
        FROM (SELECT clm_dtl_member_nbr_sk FROM aw_serverless_stable_catalog.aibi_member_claims.fact_claim_detail_v11 GROUP BY clm_dtl_member_nbr_sk HAVING SUM(clm_dtl_paid_amt) > 10000) x
      validated: true
      validation_row_count: 45
      manual_implementation_notes: Add as a named SQL dataset in the dashboard. Cannot be a metric view measure because HAVING threshold is required.
    - kpi: W-1
      name: Rolling 3-Month PMPM
      reason: Requires trailing window over monthly cross-fact numerator and denominator.
      status: NOT_IMPLEMENTED
      documentation_only: true
      sql: |
        WITH monthly AS (SELECT COALESCE(paid.service_month, enr.service_month) AS service_month, paid.total_paid, enr.member_months FROM (SELECT DATE_TRUNC('MONTH', clm_dtl_specific_dos_date) AS service_month, SUM(clm_dtl_paid_amt) AS total_paid FROM aw_serverless_stable_catalog.aibi_member_claims.fact_claim_detail_v11 GROUP BY DATE_TRUNC('MONTH', clm_dtl_specific_dos_date)) paid FULL OUTER JOIN (SELECT DATE_TRUNC('MONTH', mbr_enr_effective_date) AS service_month, COUNT(DISTINCT CONCAT(CAST(member_sk AS STRING), '-', CAST(DATE_TRUNC('MONTH', mbr_enr_effective_date) AS STRING))) AS member_months FROM aw_serverless_stable_catalog.aibi_member_claims.fact_member_enrollment_v11 GROUP BY DATE_TRUNC('MONTH', mbr_enr_effective_date)) enr ON paid.service_month = enr.service_month)
        SELECT service_month, SUM(total_paid) OVER (ORDER BY service_month ROWS BETWEEN 2 PRECEDING AND CURRENT ROW) / NULLIF(SUM(member_months) OVER (ORDER BY service_month ROWS BETWEEN 2 PRECEDING AND CURRENT ROW), 0) AS rolling_3m_pmpm FROM monthly
      validated: true
      validation_row_count: 60
      manual_implementation_notes: Add as a named SQL dataset in the dashboard. Cannot be a metric view measure because it requires window semantics and cross-fact aggregates.
    - kpi: W-2
      name: MoM Active Member Growth
      reason: Requires LAG() over monthly active member counts.
      status: NOT_IMPLEMENTED
      documentation_only: true
      sql: |
        WITH monthly AS (SELECT DATE_TRUNC('MONTH', mbr_enr_effective_date) AS service_month, COUNT(DISTINCT member_sk) AS active_members FROM aw_serverless_stable_catalog.aibi_member_claims.fact_member_enrollment_v11 WHERE is_active = true GROUP BY DATE_TRUNC('MONTH', mbr_enr_effective_date))
        SELECT service_month, (active_members - LAG(active_members) OVER (ORDER BY service_month)) / NULLIF(LAG(active_members) OVER (ORDER BY service_month), 0) AS mom_growth FROM monthly
      validated: true
      validation_row_count: 60
      manual_implementation_notes: Add as a named SQL dataset in the dashboard. Cannot be a metric view measure because it requires LAG().
self_check:
  fact_tables_in_schema: [fact_claim_detail_v11, fact_claim_header_v11, fact_member_enrollment_v11]
  fact_tables_with_ready_kpis: [fact_claim_detail_v11, fact_member_enrollment_v11]
  fact_tables_with_metric_views: [fact_claim_detail_v11, fact_member_enrollment_v11]
  fact_tables_without_metric_views_for_ready_kpis: []
