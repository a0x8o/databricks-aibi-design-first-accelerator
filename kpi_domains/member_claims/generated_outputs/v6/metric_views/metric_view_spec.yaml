metric_views:
  - name: member_claims_metric_view_v6
    catalog: aw_serverless_stable_catalog
    schema: aibi_member_claims
    sql_fqn: aw_serverless_stable_catalog.aibi_member_claims.member_claims_metric_view_v6
    yaml:
      version: 1.1
      comment: Claim service line metric view for member claims financial, utilization, denial, and clean-claim KPIs.
      source: aw_serverless_stable_catalog.aibi_member_claims.fact_claim_detail_v6
      fields:
        - name: service_month
          expr: DATE_TRUNC('MONTH', clm_dtl_specific_dos_date)
          comment: Common month dimension based on claim detail service date.
        - name: service_date
          expr: clm_dtl_specific_dos_date
          comment: Claim detail service date.
        - name: claim_type
          expr: clm_dtl_claim_type
          comment: Claim type from claim detail.
        - name: benefit_category
          expr: clm_dtl_benefit_category
          comment: Benefit category from claim detail.
        - name: benefit_level
          expr: clm_dtl_benefit_level
          comment: Benefit level from claim detail.
        - name: line_status
          expr: clm_dtl_line_status
          comment: Claim line status.
        - name: clean_claim_indicator
          expr: clm_dtl_clean_claim_ind
          comment: Clean claim indicator.
        - name: place_of_service
          expr: clm_dtl_place_of_service
          comment: Place of service code.
        - name: rendering_provider_type
          expr: clm_dtl_rendering_provider_type
          comment: Rendering provider type.
        - name: rendering_provider_specialty
          expr: clm_dtl_rendering_provider_spec
          comment: Rendering provider specialty.
        - name: adjudication_status
          expr: clm_dtl_adjudication_status
          comment: Claim adjudication status.
        - name: procedure_code
          expr: clm_dtl_procedure_code
          comment: Procedure code.
        - name: revenue_code
          expr: clm_dtl_revenue_code
          comment: Revenue code.
      measures:
        - name: Total Claims
          expr: COUNT(DISTINCT clm_dtl_claim_id)
          comment: Count of distinct claims.
          format:
            type: number
            decimal_places:
              type: exact
              places: 0
        - name: Total Claim Lines
          expr: COUNT(*)
          comment: Count of claim service lines.
          format:
            type: number
            decimal_places:
              type: exact
              places: 0
        - name: Total Paid Amount
          expr: SUM(clm_dtl_paid_amt)
          comment: Sum of paid amount across claim lines.
          format:
            type: currency
            currency_code: USD
            decimal_places:
              type: exact
              places: 2
        - name: Average Paid per Claim
          expr: MEASURE(`Total Paid Amount`) / NULLIF(MEASURE(`Total Claims`), 0)
          comment: Total paid amount divided by distinct claims.
          format:
            type: currency
            currency_code: USD
            decimal_places:
              type: exact
              places: 2
        - name: Denied Lines
          expr: SUM(CASE WHEN lower(clm_dtl_adjudication_status) LIKE '%den%' OR lower(clm_dtl_line_status) LIKE '%den%' THEN 1 ELSE 0 END)
          comment: Claim lines classified as denied from adjudication or line status.
          format:
            type: number
            decimal_places:
              type: exact
              places: 0
        - name: Denial Rate
          expr: MEASURE(`Denied Lines`) / NULLIF(MEASURE(`Total Claim Lines`), 0)
          comment: Denied lines divided by total claim lines.
          format:
            type: percentage
            decimal_places:
              type: exact
              places: 2
        - name: Clean Lines
          expr: SUM(CASE WHEN lower(clm_dtl_clean_claim_ind) IN ('y','yes','true','1','clean') THEN 1 ELSE 0 END)
          comment: Claim lines marked as clean.
          format:
            type: number
            decimal_places:
              type: exact
              places: 0
        - name: Clean Claim Rate
          expr: MEASURE(`Clean Lines`) / NULLIF(MEASURE(`Total Claim Lines`), 0)
          comment: Clean claim lines divided by total claim lines.
          format:
            type: percentage
            decimal_places:
              type: exact
              places: 2
        - name: Total Billed Amount
          expr: SUM(clm_dtl_billed_amt)
          comment: Sum of billed amount across claim lines.
          format:
            type: currency
            currency_code: USD
            decimal_places:
              type: exact
              places: 2
        - name: Payment-to-Billed Ratio
          expr: MEASURE(`Total Paid Amount`) / NULLIF(MEASURE(`Total Billed Amount`), 0)
          comment: Paid amount divided by billed amount.
          format:
            type: percentage
            decimal_places:
              type: exact
              places: 2
        - name: Total Allowed Amount
          expr: SUM(clm_dtl_allowed_amt)
          comment: Sum of allowed amount across claim lines.
          format:
            type: currency
            currency_code: USD
            decimal_places:
              type: exact
              places: 2
        - name: Payment-to-Allowed Ratio
          expr: MEASURE(`Total Paid Amount`) / NULLIF(MEASURE(`Total Allowed Amount`), 0)
          comment: Paid amount divided by allowed amount.
          format:
            type: percentage
            decimal_places:
              type: exact
              places: 2
        - name: Unique Claim Members
          expr: COUNT(DISTINCT clm_dtl_member_nbr_sk)
          comment: Count of distinct member keys present on claim detail.
          format:
            type: number
            decimal_places:
              type: exact
              places: 0
        - name: Average Paid per Member
          expr: MEASURE(`Total Paid Amount`) / NULLIF(MEASURE(`Unique Claim Members`), 0)
          comment: Total paid amount divided by unique claim members.
          format:
            type: currency
            currency_code: USD
            decimal_places:
              type: exact
              places: 2
        - name: Claims per Member
          expr: MEASURE(`Total Claims`) / NULLIF(MEASURE(`Unique Claim Members`), 0)
          comment: Distinct claims divided by unique claim members.
          format:
            type: number
            decimal_places:
              type: exact
              places: 2
        - name: Lines per Claim
          expr: MEASURE(`Total Claim Lines`) / NULLIF(MEASURE(`Total Claims`), 0)
          comment: Claim service lines divided by distinct claims.
          format:
            type: number
            decimal_places:
              type: exact
              places: 2
        - name: Inpatient Paid Amount
          expr: SUM(CASE WHEN lower(clm_dtl_claim_type) LIKE '%institutional%' OR lower(clm_dtl_claim_type) LIKE '%inpatient%' THEN clm_dtl_paid_amt ELSE 0 END)
          comment: Paid amount for institutional or inpatient claim types.
          format:
            type: currency
            currency_code: USD
            decimal_places:
              type: exact
              places: 2
        - name: Outpatient Paid Amount
          expr: SUM(CASE WHEN lower(clm_dtl_claim_type) LIKE '%professional%' OR lower(clm_dtl_claim_type) LIKE '%outpatient%' THEN clm_dtl_paid_amt ELSE 0 END)
          comment: Paid amount for professional or outpatient claim types.
          format:
            type: currency
            currency_code: USD
            decimal_places:
              type: exact
              places: 2
  - name: member_claims_enrollment_metric_view_v6
    catalog: aw_serverless_stable_catalog
    schema: aibi_member_claims
    sql_fqn: aw_serverless_stable_catalog.aibi_member_claims.member_claims_enrollment_metric_view_v6
    yaml:
      version: 1.1
      comment: Enrollment grain metric view for active members, enrollment distribution, member months, and member geography.
      source: aw_serverless_stable_catalog.aibi_member_claims.fact_member_enrollment_v6
      fields:
        - name: service_month
          expr: DATE_TRUNC('MONTH', mbr_enr_effective_date)
          comment: Common month dimension based on enrollment effective date.
        - name: effective_date
          expr: mbr_enr_effective_date
          comment: Enrollment effective date.
        - name: termination_date
          expr: mbr_enr_termination_date
          comment: Enrollment termination date.
        - name: line_of_business
          expr: mbr_enr_line_of_business
          comment: Enrollment line of business.
        - name: enrollment_status
          expr: mbr_enr_status
          comment: Enrollment status.
        - name: plan_id
          expr: mbr_enr_plan_id
          comment: Enrollment plan identifier.
        - name: group_name
          expr: mbr_enr_group_name
          comment: Enrollment group name.
        - name: member_state
          expr: member.mbr_state
          comment: Member state from the member dimension.
        - name: member_zip_code
          expr: member.mbr_zip_code
          comment: Member ZIP code from the member dimension.
        - name: member_sex
          expr: member.mbr_sex
          comment: Member sex from the member dimension.
        - name: member_race
          expr: member.mbr_race
          comment: Member race from the member dimension.
      measures:
        - name: New Member Enrollment
          expr: COUNT(DISTINCT CASE WHEN mbr_enr_effective_date IS NOT NULL THEN member_sk END)
          comment: Count distinct members entering coverage in the selected period.
          format:
            type: number
            decimal_places:
              type: exact
              places: 0
        - name: Active Members
          expr: COUNT(DISTINCT member_sk)
          comment: Count of distinct members by enrollment line of business or other enrollment dimensions.
          format:
            type: number
            decimal_places:
              type: exact
              places: 0
        - name: Active Enrolled Members
          expr: COUNT(DISTINCT CASE WHEN is_active THEN member_sk END)
          comment: Count of active enrolled members.
          format:
            type: number
            decimal_places:
              type: exact
              places: 0
        - name: Member Months
          expr: COUNT(DISTINCT CONCAT(CAST(member_sk AS STRING), '-', CAST(DATE_TRUNC('MONTH', mbr_enr_effective_date) AS STRING)))
          comment: Distinct member-month exposure from enrollment records.
          format:
            type: number
            decimal_places:
              type: exact
              places: 0
        - name: Members by Geography
          expr: COUNT(DISTINCT member_sk)
          comment: Distinct active member population intended for grouping by member_state or member_zip_code.
          format:
            type: number
            decimal_places:
              type: exact
              places: 0
      joins:
        - name: member
          source: aw_serverless_stable_catalog.aibi_member_claims.dim_member_v6
          'on': source.member_sk = member.member_sk
          rely:
            at_most_one_match: true
