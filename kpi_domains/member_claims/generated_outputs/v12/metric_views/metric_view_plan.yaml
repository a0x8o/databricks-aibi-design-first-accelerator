metric_view_plan:
  total_kpis: 23
  implementable_in_metric_views: 14
  not_implemented: 2
  skipped: 7
  metric_views:
    - name: member_claims_metric_view_v12
      sql_fqn: "`aw_serverless_stable_catalog`.`aibi_member_claims`.`member_claims_metric_view_v12`"
      source: fact_claim_detail_v12
      source_fqn: aw_serverless_stable_catalog.aibi_member_claims.fact_claim_detail_v12
      primary: true
      intermediate_view: null
      grain: one row represents a claim detail service line
      kpis: [C-1, C-2, C-3, C-4, ADD-1, ADD-2, ADD-3, ADD-4, ADD-5, ADD-6, ADD-7, ADD-8, ADD-9]
    - name: member_claims_enrollment_metric_view_v12
      sql_fqn: "`aw_serverless_stable_catalog`.`aibi_member_claims`.`member_claims_enrollment_metric_view_v12`"
      source: fact_member_enrollment_v12
      source_fqn: aw_serverless_stable_catalog.aibi_member_claims.fact_member_enrollment_v12
      primary: false
      intermediate_view: null
      grain: one row represents a member enrollment record or coverage event
      kpis: [M-1, M-2]
    - name: member_claims_member_metric_view_v12
      sql_fqn: "`aw_serverless_stable_catalog`.`aibi_member_claims`.`member_claims_member_metric_view_v12`"
      source: dim_member_v12
      source_fqn: aw_serverless_stable_catalog.aibi_member_claims.dim_member_v12
      primary: false
      intermediate_view: null
      grain: one row represents a current member dimension record
      kpis: [M-3]
  not_implemented:
    - kpi: MC-4
      name: High-Cost Member Count
      reason: Requires pre-aggregation to member spend and HAVING threshold; metric view measure syntax cannot express a member-level HAVING threshold over line-level spend safely.
      status: NOT_IMPLEMENTED
      documentation_only: true
      sql: |
        SELECT COUNT(*) AS high_cost_member_count
        FROM (
          SELECT clm_dtl_member_nbr_sk, SUM(clm_dtl_paid_amt) AS member_paid
          FROM aw_serverless_stable_catalog.aibi_member_claims.fact_claim_detail_v12
          GROUP BY clm_dtl_member_nbr_sk
          HAVING SUM(clm_dtl_paid_amt) > 10000
        ) h
      validated: true
      validation_row_count: 45
      manual_implementation_notes: |
        To implement: Add as a named SQL dataset in the dashboard. Cannot be a metric view measure because the KPI requires pre-aggregating spend by member and filtering with HAVING.
    - kpi: W-2
      name: MoM Active Member Growth
      reason: Requires LAG window function over monthly active member counts; document as SQL because offset windows are not implemented in this metric-view design.
      status: NOT_IMPLEMENTED
      documentation_only: true
      sql: |
        SELECT service_month, active_members, prior_month_members,
               (active_members - prior_month_members) / NULLIF(prior_month_members, 0) AS mom_growth
        FROM (
          SELECT service_month, active_members,
                 LAG(active_members) OVER (ORDER BY service_month) AS prior_month_members
          FROM (
            SELECT date_trunc('MONTH', mbr_enr_effective_date) AS service_month,
                   COUNT(DISTINCT member_sk) AS active_members
            FROM aw_serverless_stable_catalog.aibi_member_claims.fact_member_enrollment_v12
            GROUP BY date_trunc('MONTH', mbr_enr_effective_date)
          ) m
        ) g
      validated: true
      validation_row_count: 60
      manual_implementation_notes: |
        To implement: Add as a named SQL dataset in the dashboard. Cannot be a standard metric view measure because it requires LAG over monthly aggregates.
  skipped_kpis:
    - {kpi: MC-1, name: PMPM, status: SKIPPED_UNSAFE_GRAIN, reason: Requires paid numerator from claim detail and member-month denominator from enrollment; no safe single source or validated pre-aggregated bridge at common grain.}
    - {kpi: MC-2, name: Claims per 1000 Members, status: SKIPPED_UNSAFE_GRAIN, reason: Requires distinct claims from claim detail and member-month denominator from enrollment at a common month grain; no safe single metric-view source.}
    - {kpi: MC-3, name: Utilization Rate, status: SKIPPED_UNSAFE_GRAIN, reason: Requires members with claims and active enrolled members across distinct fact grains and incompatible member key types.}
    - {kpi: W-1, name: Rolling 3-Month PMPM, status: SKIPPED_UNSUPPORTED_METRIC_VIEW_FEATURE, reason: Requires rolling numerator/denominator across claim and enrollment grains; unsafe without a pre-aggregated common-grain source.}
    - {kpi: ADD-10, name: Participating Provider Rate, status: SKIPPED_UNRESOLVED_RELATIONSHIP, reason: No semantic_model relationship from claim facts to dim_provider; provider participation columns not safely reachable.}
    - {kpi: M-1, name: New Member Enrollment, status: IMPLEMENTED_IN_ENROLLMENT_MV, reason: Implemented in enrollment metric view using mbr_enr_effective_date as service_month.}
    - {kpi: M-3, name: Members by Geography, status: IMPLEMENTED_IN_MEMBER_MV, reason: Implemented in member metric view sourced directly from dim_member_v12.}
grain_analysis:
  fact_tables_in_schema: [fact_member_enrollment_v12, fact_claim_header_v12, fact_claim_detail_v12]
  fact_tables_with_ready_kpis:
    fact_claim_detail_v12: 13
    fact_member_enrollment_v12: 2
  fact_or_dimension_tables_with_metric_views: [fact_claim_detail_v12, fact_member_enrollment_v12, dim_member_v12]
  missed_ready_fact_grains: []
