metric_views:
  - name: member_claims_metric_view_v3
    yaml:
      version: 1.1
      comment: Member claims claim-detail grain metric view for financial, operational, utilization, and provider KPIs.
      source: aw_serverless_stable_catalog.aibi_member_claims.fact_claim_detail_v3
      fields:
        - name: service_date
          expr: clm_dtl_specific_dos_date
          comment: Claim line service date.
        - name: service_month
          expr: date_trunc('MONTH', clm_dtl_specific_dos_date)
          comment: Common month dimension derived from claim line service date.
        - name: claim_type
          expr: clm_dtl_claim_type
          comment: Claim type from the claim detail line.
        - name: line_status
          expr: clm_dtl_line_status
          comment: Claim line processing status.
        - name: clean_claim_indicator
          expr: clm_dtl_clean_claim_ind
          comment: Clean claim indicator from the claim detail line.
        - name: place_of_service
          expr: clm_dtl_place_of_service
          comment: Place of service code.
        - name: procedure_code
          expr: clm_dtl_procedure_code
          comment: Procedure code for the service line.
        - name: participating_provider
          expr: clm_dtl_participating_provider
          comment: Participating provider indicator or category.
        - name: rendering_provider_specialty
          expr: clm_dtl_rendering_provider_spec
          comment: Rendering provider specialty.
        - name: adjudication_status
          expr: clm_dtl_adjudication_status
          comment: Claim line adjudication status.
      measures:
        - name: total_claims
          expr: COUNT(DISTINCT clm_dtl_claim_id)
          comment: Count of distinct claims represented by claim detail lines.
          format: {type: number, decimal_places: {type: exact, places: 0}}
        - name: total_claim_lines
          expr: COUNT(*)
          comment: Count of claim detail service lines.
          format: {type: number, decimal_places: {type: exact, places: 0}}
        - name: total_paid_amount
          expr: SUM(clm_dtl_paid_amt)
          comment: Total paid amount summed across claim detail lines.
          format: {type: currency, currency_code: USD, decimal_places: {type: exact, places: 2}}
        - name: average_paid_per_claim
          expr: MEASURE(`total_paid_amount`) / NULLIF(MEASURE(`total_claims`), 0)
          comment: Total paid amount divided by distinct claims.
          format: {type: currency, currency_code: USD, decimal_places: {type: exact, places: 2}}
        - name: denied_lines
          expr: SUM(CASE WHEN lower(clm_dtl_line_status) LIKE '%den%' OR lower(clm_dtl_adjudication_status) LIKE '%den%' THEN 1 ELSE 0 END)
          comment: Count of denied claim lines based on line or adjudication status.
          format: {type: number, decimal_places: {type: exact, places: 0}}
        - name: denial_rate
          expr: MEASURE(`denied_lines`) / NULLIF(MEASURE(`total_claim_lines`), 0)
          comment: Denied claim lines divided by total claim lines.
          format: {type: percentage, decimal_places: {type: exact, places: 2}}
        - name: clean_lines
          expr: SUM(CASE WHEN lower(clm_dtl_clean_claim_ind) IN ('y', 'yes', 'true', '1', 'clean') THEN 1 ELSE 0 END)
          comment: Count of clean claim lines.
          format: {type: number, decimal_places: {type: exact, places: 0}}
        - name: clean_claim_rate
          expr: MEASURE(`clean_lines`) / NULLIF(MEASURE(`total_claim_lines`), 0)
          comment: Clean claim lines divided by total claim lines.
          format: {type: percentage, decimal_places: {type: exact, places: 2}}
        - name: total_billed_amount
          expr: SUM(clm_dtl_billed_amt)
          comment: Total billed amount summed across claim lines.
          format: {type: currency, currency_code: USD, decimal_places: {type: exact, places: 2}}
        - name: payment_to_billed_ratio
          expr: MEASURE(`total_paid_amount`) / NULLIF(MEASURE(`total_billed_amount`), 0)
          comment: Paid amount divided by billed amount.
          format: {type: percentage, decimal_places: {type: exact, places: 2}}
        - name: total_allowed_amount
          expr: SUM(clm_dtl_allowed_amt)
          comment: Total allowed amount summed across claim lines.
          format: {type: currency, currency_code: USD, decimal_places: {type: exact, places: 2}}
        - name: payment_to_allowed_ratio
          expr: MEASURE(`total_paid_amount`) / NULLIF(MEASURE(`total_allowed_amount`), 0)
          comment: Paid amount divided by allowed amount.
          format: {type: percentage, decimal_places: {type: exact, places: 2}}
        - name: unique_members
          expr: COUNT(DISTINCT clm_dtl_member_nbr_sk)
          comment: Count of unique member identifiers present on claim detail lines.
          format: {type: number, decimal_places: {type: exact, places: 0}}
        - name: average_paid_per_member
          expr: MEASURE(`total_paid_amount`) / NULLIF(MEASURE(`unique_members`), 0)
          comment: Paid amount divided by unique members with claim lines.
          format: {type: currency, currency_code: USD, decimal_places: {type: exact, places: 2}}
        - name: claims_per_member
          expr: MEASURE(`total_claims`) / NULLIF(MEASURE(`unique_members`), 0)
          comment: Distinct claims divided by unique members with claim lines.
          format: {type: number, decimal_places: {type: exact, places: 2}}
        - name: lines_per_claim
          expr: MEASURE(`total_claim_lines`) / NULLIF(MEASURE(`total_claims`), 0)
          comment: Claim detail lines divided by distinct claims.
          format: {type: number, decimal_places: {type: exact, places: 2}}
        - name: inpatient_paid_amount
          expr: SUM(CASE WHEN lower(clm_dtl_claim_type) LIKE '%institutional%' OR lower(clm_dtl_claim_type) LIKE '%inpatient%' THEN clm_dtl_paid_amt ELSE 0 END)
          comment: Paid amount for institutional or inpatient-style claim types.
          format: {type: currency, currency_code: USD, decimal_places: {type: exact, places: 2}}
        - name: outpatient_paid_amount
          expr: SUM(CASE WHEN lower(clm_dtl_claim_type) LIKE '%professional%' OR lower(clm_dtl_claim_type) LIKE '%outpatient%' THEN clm_dtl_paid_amt ELSE 0 END)
          comment: Paid amount for professional or outpatient-style claim types.
          format: {type: currency, currency_code: USD, decimal_places: {type: exact, places: 2}}
        - name: participating_provider_paid
          expr: SUM(CASE WHEN lower(clm_dtl_participating_provider) IN ('y', 'yes', 'true', '1', 'par', 'participating') THEN clm_dtl_paid_amt ELSE 0 END)
          comment: Paid amount associated with participating providers.
          format: {type: currency, currency_code: USD, decimal_places: {type: exact, places: 2}}
        - name: participating_provider_rate
          expr: MEASURE(`participating_provider_paid`) / NULLIF(MEASURE(`total_paid_amount`), 0)
          comment: Participating provider paid amount divided by total paid amount.
          format: {type: percentage, decimal_places: {type: exact, places: 2}}
  - name: member_claims_enrollment_metric_view_v3
    yaml:
      version: 1.1
      comment: Member claims enrollment-grain metric view for member enrollment, line of business, and geography KPIs.
      source: aw_serverless_stable_catalog.aibi_member_claims.fact_member_enrollment_v3
      fields:
        - name: service_date
          expr: source.mbr_enr_effective_date
          comment: Enrollment effective date exposed as common service date.
        - name: service_month
          expr: date_trunc('MONTH', source.mbr_enr_effective_date)
          comment: Common month dimension derived from enrollment effective date.
        - name: line_of_business
          expr: source.mbr_enr_line_of_business
          comment: Enrollment line of business.
        - name: enrollment_status
          expr: source.mbr_enr_status
          comment: Enrollment status.
        - name: product_id
          expr: source.mbr_enr_product_id
          comment: Enrollment product identifier.
        - name: termination_reason
          expr: source.mbr_enr_termination_reason
          comment: Enrollment termination reason.
        - name: member_state
          expr: member.mbr_state
          comment: Member state from the member dimension.
        - name: member_zip_code
          expr: member.mbr_zip_code
          comment: Member ZIP code from the member dimension.
        - name: member_sex
          expr: member.mbr_sex
          comment: Member sex from the member dimension.
      measures:
        - name: new_member_enrollments
          expr: COUNT(DISTINCT source.member_sk)
          comment: Distinct members entering coverage by enrollment effective date period.
          format: {type: number, decimal_places: {type: exact, places: 0}}
        - name: active_members
          expr: COUNT(DISTINCT CASE WHEN source.is_active THEN source.member_sk END)
          comment: Distinct active members in enrollment records.
          format: {type: number, decimal_places: {type: exact, places: 0}}
        - name: enrollment_records
          expr: COUNT(DISTINCT source.enrollment_sk)
          comment: Count of distinct enrollment records.
          format: {type: number, decimal_places: {type: exact, places: 0}}
        - name: members_by_geography
          expr: COUNT(DISTINCT source.member_sk)
          comment: Distinct enrolled members for geography slices.
          format: {type: number, decimal_places: {type: exact, places: 0}}
      joins:
        - name: member
          source: aw_serverless_stable_catalog.aibi_member_claims.dim_member_v3
          'on': source.member_sk = member.member_sk
          rely:
            at_most_one_match: true
